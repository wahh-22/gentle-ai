# Tasks: RDD Root Simplification — Wave 4 (Thin Consumers)

## Gate

HARD-GATED: chains on Wave 3's final slice (feature-branch-chain, tracker
`feature/rdd-root-simplification`). Wave 3 is not yet on `main`
(`review_offer.go`/`review_core.go`/`authority_store.go` absent at `d591f4cf`).
Do not open Wave 4 PR0 until Wave 3's last slice merges to the tracker branch.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | S1 ~300, S2 ~350, S3 ~850, S4 ~650, S5 ~900, S6 ~500, S7 ~750 (total ~4300) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR0 → S1 → S2 → S3 → S4 → S5 → S6 → S7 |
| Delivery strategy | auto-chain |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | PR | Focused test | Harness | Rollback |
|---|---|---|---|---|---|
| PR0 | Land W4 SDD artifacts + Wave 3 archive move | tracker base | N/A (docs) | N/A — SDD artifacts only | Revert `openspec/changes/rdd-root-simplification-wave4/**` |
| S1 | Adapter behavioral-depth trace, inventory, forbidden-construction guard, pin fix | PR0-base | `go test ./internal/agents/... -run ForbiddenConstruction` | N/A — static grep/trace, no runtime scenario | Revert inventory edits + guard test; pin fix reverts independently |
| S2 | Decision-9 single work-unit owner (`RuntimeObjective`) | S1-base | `go test ./internal/sddstatus/... -run RuntimeObjective` | `t.TempDir()` CAS/replay integration | Revert `BeginAttemptRequest` field collapse |
| S3 | Offer call site FIRST + pre-verify routing removal + decline + AST/call-graph absence guard | S2-base | `go test ./internal/sddstatus/... ./internal/cli/... -run OfferAbsence` | OFF-mode bench apply→verify→archive | Revert offer call site; restore `applyPreVerify*Routing` |
| S4 | Transport capability admission (second) | S3-base | `go test ./internal/agents/capabilitymanifest/... -run ReviewTransport` | unsupported-transport bench denial journey | Revert `ContractReviewTransportV1` claim + admission check |
| S5 | `ReceiptRef` record + legacy read-only projection | S4-base | `go test ./internal/reviewtransaction/... -run ReceiptRef` | `t.TempDir()` golden byte comparison of `binding.json` | Revert `receipt_ref.go`/`legacy_binding_read.go`; ledger reverts to `BindingRevision` |
| S6 | Targeted re-verify three-branch scoping | S5-base | `go test ./internal/cli/... -run TargetedReverify` | staged/`commit -a`/empty-index/unborn-HEAD bench matrix | Revert re-verify branch logic |
| S7 | Mirror writer deletion + adapter/asset/golden updates (lands last) | S6-base | `go test ./internal/sddstatus/... -run BindingWriterRemoved` | 5-gate golden regeneration `-update` then verify | Restore `review_binding.go` writer half from git history |

## Phase 1 (PR0): SDD Artifacts

- [x] 1.1 Land `openspec/changes/rdd-root-simplification-wave4/{proposal,specs,design,tasks}.md` (already written). Copied byte-identical (`diff -r` clean) into worktree `rdd-wave4` on `docs/rdd-wave4-sdd-artifacts` at chain base `feat/rdd-wave4-base` (413d3967).
- [ ] 1.2 Archive Wave 3 (`openspec/changes/rdd-root-simplification-wave3/**` → `openspec/specs/`) when its turn comes, mirroring prior wave pattern. DEFERRED: Wave 3 has not landed on the tracker branch yet — `feat/rdd-wave3-s5-taxonomy-offer-bench` was still receiving an in-progress fix commit (default-deny C5 + ReviewCore-owned finalize C6) at the time Wave 4 apply started. Archiving now would archive an unstable/incomplete Wave 3 spec. Wave 4 proceeds on top of Wave 3's code (present at chain tip 413d3967) per explicit orchestrator instruction, but this task stays open until Wave 3's own archive phase runs.

## Phase 2 (S1): Adapter Trace + Guard + Pin Fix

- [x] 2.1 Run the Wave-0 CON-09/10/11 trace per adapter: `codegraph_explore` blast radius, grep Go package + bundled asset tree for the four forbidden constructions (CLI flag literals, revision/`expected-revision`, target/lineage identity, binding JSON). Confirmed via `rg`: zero review references in `internal/agents/{opencode,pi,claude}` (one unrelated comment hit in `capabilitymanifest/manifest.go`); violations found in `internal/assets/opencode/plugins/review-result-artifacts.ts` (ReviewBinding/admissionRecoveryKey/recovery budget) and in the `sdd-apply.md` contract pin (see 2.5).
- [x] 2.2 Record verdicts in `docs/architecture/rdd-ownership-inventory.md`: amended CON-09/10/11 evidence columns with the traced verdicts, deleted the "behavioral depth" gap bullet.
- [x] 2.3 RED: forbidden-construction guard test encoding the CON-09/10/11 grep set so the trace cannot silently rot. Genuine compile-fail RED captured (`undefined: scanAdapterForbiddenConstructions`, `undefined: scanAdapterForbiddenConstructionsFile`).
- [x] 2.4 GREEN: 2.3 passes against current adapters (`internal/agents/opencode/adapter.go`, `pi/adapter.go`, `claude/adapter.go` — all clean; violations live in the bundled assets, tracked by S3/S7). `go test ./internal/agents/... -run ForbiddenConstruction` — PASS, 5 production files scanned across opencode/pi/claude.
- [x] 2.5 Cross-slice fix (rides S1 per CI-exact-head rule): corrected `gentle-ai.review-integration/v1` → `/v2` pin in `internal/assets/claude/commands/sdd-apply.md` and `internal/assets/opencode/commands/sdd-apply.md`; resolves design's open pin-placement question. Golden regeneration (`go test ./internal/components/ -run Golden -update`) confirmed byte-diff limited to the pin change; rerun without `-update` is GREEN.
- [x] 2.6 `scripts/deadcode-ratchet.sh --update` for inventory/guard changes. Net-zero delta: 233 entries before and after (no new unreachable functions).

## Phase 3 (S2): Decision-9 Single Owner

- [x] 3.1 RED: work-unit-owner uniqueness test — `RuntimeObjective` is the sole scope owner; no parallel struct exists. `TestRuntimeObjectiveIsSoleWorkUnitScopeOwner` (reflect-based) — genuine RED captured against the pre-collapse struct (5 fields, want 1).
- [x] 3.2 Collapse `CompactAcquireRequest`'s `WorkUnit`/`EvidenceGoal`/`MaxAttempts`/`MaxChangedLines` into `BeginAttemptRequest` fields (`runtime_ledger.go`); `runtime_compact.go` becomes projection-only through `normalizeBeginAttemptRequest`. Implemented via anonymous embedding (`CompactAcquireRequest struct { BeginAttemptRequest }`); updated 3 call sites (internal/cli/sdd_attempt.go, internal/sddstatus/runtime_compact_test.go, internal/sddstatus/runtime_objective_advance_test.go x2).
- [x] 3.3 GREEN: 3.1 passes; advance-vs-reset distinction (#2133/#2151) defined once at the objective. Confirmed via `rg`: `runtimeObjectiveAdvanceAdmissible` (runtime_ledger.go:782) and `runtimeResetStructurallyPermitted` (runtime_ledger.go:1720) each have exactly one definition in the package — no duplicate logic. Full `internal/sddstatus` Compact/Acquire/Objective-scoped suite: 17 tests PASS.
- [x] 3.4 Rewrite the conditional CON-08 owner row at `docs/architecture/rdd-ownership-inventory.md` to name `RuntimeObjective` unconditionally (drop "only if" wording) per maintainer-confirmed decision 9.
- [x] 3.5 `scripts/deadcode-ratchet.sh --update` for removed `CompactAcquireRequest` fields. Net-zero: 233/233 entries.

## Phase 4 (S3): Offer Call Site + Absence Guard (FIRST per spec sequencing)

- [x] 4.1 RED: static AST/call-graph guard asserting zero call edges into offer/`ReviewCore` symbols from any SDD apply/verify/archive path — must fail while pre-verify routing still exists. Genuine compile-fail RED captured in both `internal/sddstatus` and `internal/cli` (guard test files reference `reviewEntryHook`/`offerEntryHook` before the door files exist: `undefined: reviewEntryHook` / `undefined: offerEntryHook`).
- [x] 4.2 Create `internal/sddstatus/review_door.go` (`reviewEntryHook`) and `internal/cli/review_offer_door.go` (`offerEntryHook`, scoped to the verify-success-exit path only; the ~30 other non-test `internal/cli` files importing `reviewtransaction` for explicit `review` subcommands stay out of scope). Both guards GREEN: `TestReviewOfferAbsenceGuardHoldsForProductionFiles` (8 sddstatus files) and `TestReviewOfferAbsenceGuardHoldsForScopedCLIFiles` (sdd_attempt.go/sdd_status.go/sdd_verify_validate.go).
- [x] 4.3 Remove `applyPreVerifyReviewRouting` (`status.go:829`), `applyPreVerifyCompactBridgeRouting` (`status.go:857`), and all four call sites (`:487`, `:490`, `:770`, `:772`). Also removed `resolveEngramStatus`'s now-dead local bridge-discovery block (its `bridge` var had no remaining reader once both routing-applier calls were gone — unlike `Resolve()`, `resolveEngramStatus` had no separate post-verify-failure `bridge.Eligible` reader). Characterized via the full pre-existing `internal/sddstatus` suite (green baseline going in); 6 tests broke as an EXPECTED, intentional consequence (they asserted the exact removed behavior) and were rewritten to assert the new default (`Verify: ready`, `next: verify`, immediately once apply is done, regardless of review-transaction/compact-bridge presence, validity, or ambiguity) — see apply-progress for the full list and rationale per test.
- [x] 4.4 Wire the offer call site — **amended by the orchestrator 2026-08-03** (design.md "Amendment (orchestrator-resolved): decision 3 call site"): NOT `RunSDDVerifyValidate` (stays repo-context-free, no flags added); routes through `internal/sddstatus`'s `Resolve()`/`resolveEngramStatus()` instead, via a new `Status.ReviewOffer` field and `applyReviewOfferRouting`, calling `review_door.go`'s new `reviewOfferForVerify` wrapper (fronting `reviewtransaction.OfferReviewAfterVerify`). Fires exactly when `Dependencies.Verify == DependencyAllDone` and the kill switch is on.
- [x] 4.5 Implement decline = nothing recorded: unmanaged proceed, no new state, no new persistence, does not suppress the offer for a later read. `TestReviewOfferDeclineLeavesNoStateAndDoesNotSuppressLaterOffer` — offer reappears on a second `Resolve()` call, `Dependencies.Archive` unchanged (governed entirely by the pre-existing, unrelated post-verify archive gate).
- [x] 4.6 GREEN: 4.1 passes (both packages). `.deadcode-baseline.txt` entry for `review_offer.go` REMOVED — `OfferReviewAfterVerify`/`readGlobalRDDModeForOffer` are now genuinely reachable from `./cmd/gentle-ai` (confirmed by `scripts/deadcode-ratchet.sh` reporting them "now reachable"). Two new legitimate test-support entries added (`reviewEntryHookCallCountForTest`/`resetReviewEntryHookCallCountForTest`), matching the `shadowObserverCallCountForTest` precedent — net 233/233 but qualitatively net-negative (production code wired, only test-support helpers added).
- [x] 4.7 Corroborating: OFF-mode absence counter — `reviewEntryHook` fires zero times across a simulated apply→verify→archive-pending flow (`TestReviewOfferJourneyFiresZeroTimesAcrossFullFlowWhenDisabled`). Realized as an in-process Go test, not a `bench/` black-box journey — `reviewEntryHook` is a Go-internal instrumentation point a separate bench subprocess cannot observe (documented in the amendment).
- [x] 4.8 Integration test: decline output byte-identical to OFF, realized as a same-fixture double-eval (`TestReviewOfferDeclineStatusByteIdenticalToOffOutsideOfferBlock`): same fixture, only the kill-switch toggle changed, `reviewOffer` stripped from the enabled side — byte-identical JSON.
- [x] 4.9 `scripts/deadcode-ratchet.sh --update` for door symbols and removed routing functions. 233/233 total; see 4.6 for the qualitative breakdown.

**Extended in this increment (JSON-level absence, per orchestrator amendment point 2)**: `TestReviewOfferBlockAbsentStructurallyWhenDisabled` marshals `Status` to JSON with the switch off and asserts no `"reviewOffer"` substring appears — the AST guard (4.1) proves no *code path* reaches the offer symbols; this proves the *serialized output* also carries no trace.

## Phase 5 (S4): Transport Capability Admission (second per spec sequencing)

- [x] 5.1 RED: unsupported-transport denial before any authority/tier/lens/budget/collection state exists (store inspection asserting empty authority root). `TestUnsupportedReviewTransportCapabilityStopsBeforeAnyAuthority` — genuine compile-fail RED captured before `authorizeReviewTransportCapability`/`reviewTransportCapabilityUnsupportedReason`/`ContractReviewTransportV1` existed; GREEN asserts `reviewtransaction.DiscoverCompactStores` finds zero stores after a Pi-agent `review start` refusal.
- [x] 5.2 RED (threat matrix — Process integration/adapters): absent claim (Pi), unrecognised claim (`unknown-runtime`), advertised-but-failing transport (self-inconsistent manifest via the `reviewTransportCapabilityForAgent` test seam) — three distinct table cases in `TestAuthorizeReviewTransportCapabilityMatrix`.
- [x] 5.3 Add `ContractReviewTransportV1` to `ContractClaims` in `internal/agents/capabilitymanifest/manifest.go`, reusing `dormant|advertised` vocabulary, canonical registry, `Validate()`, digest. Every in-repo adapter advertises it except Pi (Dormant — "declares only `AutoInstall|SystemPrompt|MCP`"). `Advertises()` extended; `wantManifestDigests` in `manifest_test.go` deliberately updated (new field content legitimately changes every agent's canonical digest).
- [x] 5.4 Wire capability admission in `internal/cli/review_facade.go`'s `review start`, BEFORE risk/tier/lens/budget/consent/freeze — positioned before the pre-existing (narrower) immutable-receipt-transport check too, since it is the broader "can this runtime participate in review at all" precondition. Gated on an agent identity actually being supplied (`--agent`); the manual/non-agent compatibility path is not gated at all.
- [x] 5.5 Per-adapter unavailable mode: Pi adapter (declares only `AutoInstall|SystemPrompt|MCP`) enters unavailable, never a self-constructed transition — proven by 5.1's empty-authority-root assertion (nothing is constructed before the refusal).
- [x] 5.6 GREEN: 5.1, 5.2 pass. Updated the pre-existing `TestUnsupportedImmutableReviewTransportStopsBeforeRepositoryOrAuthority` precedent test: Pi/unknown's `review start` sub-cases now correctly expect the new, broader `review_transport_capability_unsupported` code (caught earlier than the old narrower check); `review status` sub-cases unaffected (the new gate only runs in `review start`). Full `internal/cli` suite (180.5s) green, including `TestEveryProductionRefusalNamesResolutionOrDeclaresByDesign` (removed two invalid `refusal:by-design` markers on `%w`-wrap propagation sites — wraps are already exempt, the marker exempts nothing).
- [x] 5.7 `scripts/deadcode-ratchet.sh --update` for the new claim. No new unreachable functions.

## Phase 6 (S5): ReceiptRef Record

- [x] 6.1 RED: `ReceiptRef{Lineage, ReceiptHash}` two-field shape test, rejects extra fields. **Renamed to `SDDReceiptRef`** — `ReceiptRef{LineageID, AuthorityRevision}` already exists (Wave 3, `review_core_transition.go`, ReviewCore's terminal-transition output shape); a second, differently-shaped `ReceiptRef` in the same package is a compile-time collision, not a design ambiguity, so it was resolved by renaming SDD's terminal-pointer type rather than touching Wave 3's shipped API. `TestSDDReceiptRefIsExactlyTwoFields` (reflect field-count/name guard) + `TestSDDReceiptRefJSONRoundTripRejectsExtraFields` (`json.Decoder.DisallowUnknownFields`).
- [x] 6.2 RED: legacy binding.json read-only compat test — parses to an in-memory `SDDReceiptRef`, never rewritten, never unlinked; golden byte comparison of the untouched file. `TestParseLegacyBindingLeavesTheOnDiskFileByteIdentical` (bytes + mtime unchanged after `parseLegacyBinding`).
- [x] 6.3 Create `internal/reviewtransaction/receipt_ref.go`: `SDDReceiptRef` + `ValidateSDDReceiptRef(ctx, repo, ref) (GateResult, string, error)`. Real implementation (not a stub): resolves the repo via `SnapshotBuilder.ResolveRepositoryRoot`, loads the lineage's compact authoritative store, verifies the terminal receipt bytes hash to exactly `ref.ReceiptHash`, evaluates the post-apply gate via the existing `EvaluateCompactGate`.
- [x] 6.4 Create `internal/sddstatus/legacy_binding_read.go`: `parseLegacyBinding` wraps the UNCHANGED `parseBinding` (still owned by `review_binding.go`'s writer-half machinery — not moved) and projects its result to `reviewtransaction.SDDReceiptRef`. Performs no file I/O of its own, so it structurally cannot rewrite or unlink anything.
- [x] 6.5 **(S5b)** Add `RuntimeStatus.Receipt *SDDReceiptRef`, new `runtimeReceiptEvent` (operation `receipt/set`) in a new `internal/sddstatus/runtime_receipt.go`, **additively alongside** `BindingRevision`/`Binding *ReviewBinding` (not a literal field replacement — see deviation note below). `recordPreparedReceipt` mirrors `bindPreparedReview`'s exact CAS/locking and one-time legacy-import idempotency semantics precisely (same lock/load/prepare/reopen-and-diff/commit sequence), using `legacy_binding_read.go`'s `parseLegacyBinding` as the compatibility seam instead of the full `ReviewBinding` shape, and `receiptRefDigest` (a new sha256-of-canonical-JSON digest) as the CAS anchor in place of `ReviewBinding.Revision` (`SDDReceiptRef` has no revision field of its own — decision 1's exactly-two-fields constraint). `validateRuntimeRecordShape`'s existing 6 per-operation nil-checks extended for the new `Receipt` field; a 7th case added mirroring the `Bind` case's validation shape exactly. Characterized first: full `internal/sddstatus` suite (27s) confirmed green both before and after, with zero pre-existing tests touched or broken (purely additive — none of the 26+ existing `ReviewBinding`/`Binding`/`BindingRevision` call sites needed to change). **Deviation from the literal task text**: "replacing" was not carried out — see below.
- [x] 6.6 **(S5c')** Modify `review_gate.go`: the archive-gate evaluation path now keeps only the provider verdict for the explicit-governance branch. Not literally "`applyReviewGateEvaluation` keeps only the provider verdict" (see the applyReviewGateEvaluation deviation note under 6.7) — the discovery/unbound path already only used the provider verdict and needed no change; the explicit-governance branch (previously `validateBoundReview`/`loadEffectiveReviewBinding`+`validateRuntimeBoundCandidate`, a stored-`GateContext`-comparison check) now calls `reviewtransaction.ValidateSDDReceiptRef` directly and stores its `(result, reason)` verbatim, with no `GateContext` re-comparison at all.
- [x] 6.7 **(S5c', re-scoped per coordinator decision 2026-08-03 — see design.md's "Amendment (coordinator-resolved): decision 1 scope")** Collapsed `resolveCompactRemediationAuthority`'s bound-authority branch (previously `validateBoundReview`) into a single `ValidateSDDReceiptRef` call; `resolveReviewAuthority` itself is unchanged (it was already receipt-discovery-based, never `Binding`-based, so there was nothing in it to collapse — it remains the unbound fallback path). New `resolveGoverningReceiptRef` (`review_gate.go`) resolves the change's governing ref by reusing the UNCHANGED `loadEffectiveReviewBinding` (native `RuntimeStatus.Binding`, still populated only by the untouched `review bind-sdd` writer path, or the read-only legacy `binding.json` projection) and projecting it to `SDDReceiptRef{Lineage, ReceiptHash}` — **not** reading the ledger-native `RuntimeStatus.Receipt` field, which stays genuinely unwired in production (see below). `status.go`'s two `bindingPresent` branches (`Resolve()` and `resolveEngramStatus()`) and `resolveCompactRemediationAuthority`'s bound branch all reroute onto this ref + `ValidateSDDReceiptRef`, replacing `bindingExists`/`validateBoundReview`/`loadEffectiveReviewBinding`+`validateRuntimeBoundCandidate` there. Per the coordinator's amendment, `RuntimeStatus.Binding`/`BindingRevision`, `BindApprovedReview`/`bind-sdd`, and the entire remediation-successor CAS subsystem (`runtime_ledger.go`'s `Finish()`, `runtime_compact.go`'s `Settle()`, `validateRuntimeBoundCandidate`'s OTHER caller at `runtime_ledger.go:564`, `runtimeSelfSuccessorAvailable`, `runtimeStrandedSuccessor`) stay completely untouched, with their existing tests unmodified and green.
  - **RED evidence (genuine)**: `internal/sddstatus/review_gate_receipt_test.go` — `TestResolveGoverningReceiptRefPresenceAndAbsence` was a compile-fail RED (`undefined: resolveGoverningReceiptRef`) before the function existed. `TestBoundReviewArchiveGateIgnoresStaleGateContextViaReDerivation` is a genuine behavioral RED: it binds a real approved compact authority via `BindApprovedReview`, corrupts only the persisted `GateContext.LedgerHash` to a well-formed-but-wrong value (not `EmptyFixDeltaHash`, so `boundGateContextMatches`'s own empty-hash exemption can't mask it) via a hand-written legacy `binding.json` fixture, and asserts `status.ReviewGate.Result == GateAllow` — this FAILED against the pre-change code (`ReviewGate == nil`, correctly rejected by the old stored-context comparison) and PASSED after rerouting to `ValidateSDDReceiptRef`, which never reads `GateContext` at all (decision 1: "GateContext on the ref IS the re-derivation").
  - **Regression caught and fixed during this slice**: the first `resolveGoverningReceiptRef` implementation propagated a real error (not "absent") for repositories with no `.git` at all (pure planning/OpenSpec fixtures), because it called `loadEffectiveReviewBinding`→`OpenRuntimeStore` directly instead of pre-checking `ResolveRepositoryRoot` the way `bindingExists` always did — this broke `TestPlanningRouteRenderersOmitExpectedBlockersAndRetainGenuineBlockers` (`NextRecommended` became `resolve-blockers` instead of `propose`). Fixed by adding the same `ResolveRepositoryRoot`-first resilience `bindingExists` had; full suite green after the fix.
  - **Scope reductions, recorded explicitly (not silently dropped)**: (1) `verifyBindingLedger`'s OpenSpec findings-ledger drift check, which `validateBoundReview` re-ran on every `Resolve()` read, is no longer re-verified at read time — it is still enforced once at bind time via the unchanged `prepareApprovedReviewBinding` (`review bind-sdd`'s writer path), so ledger drift is caught at bind time, not on every subsequent status read. (2) `validateBoundReview`'s TOCTOU double-read hardening (re-reading binding/record/receipt/changeRoot and re-evaluating the gate a second time after a test hook point, then comparing byte-for-byte) has no equivalent in `ValidateSDDReceiptRef`'s single-call contract — decision 1's own premise is "the result and reason it returns are the provider's own verdict, stored verbatim," a single-evaluation trust model, not a two-phase check-then-recheck. Neither reduction broke any existing test (none of the 11 pre-existing `review_binding_test.go` Resolve()-level tests exercise "bind successfully, THEN drift the ledger, THEN re-read" — only "drift THEN bind", which the writer path still catches), but both are real, deliberate scope reductions worth flagging for Wave 7/maintainer awareness.
  - **`recordPreparedReceipt`'s write-side stays unwired, as investigated and reported**: per the coordinator's explicit escape hatch ("if some write-side helper remains unwired because its caller is the deferred CAS work, leave it baselined and say so") — the archive-gate flow does not itself create receipts; the only production receipt-governance-creation point remains `review bind-sdd` (writing `Binding`, not `Receipt`), which stays untouched per the amendment. `recordPreparedReceipt`/`readLegacyReceiptRef`/`normalizeRecordReceiptRequest`/`ReceiptRevisionConflictError.Error` (S5b's +4) remain genuinely unwired; the coordinator's "turning it net-negative" hope did not materialize because no natural production wiring point exists within this slice's scope — recorded rather than forced.
  - **Deadcode ratchet**: net **+2** (`bindingExists`, `validateBoundReview`, both in `review_binding.go`, both now genuinely orphaned — 239→241 entries). These are exactly the two symbols task 8.1's ratchet-as-arbiter pass should delete in S7 (their only callers were status.go's now-rerouted branches and `resolveCompactRemediationAuthority`'s now-rerouted bound branch). `bindingPath` (used by test fixtures and `loadEffectiveReviewBinding`'s sibling `bindingExists`, itself now orphaned) stays baselined unchanged from before.
  - **Deviation, recorded explicitly rather than silently resolved**: the coordinator's literal instruction ("replace RuntimeStatus.BindingRevision/Binding with Receipt") was not carried out — `Binding`/`BindingRevision` still exist, unchanged, still serving the pre-existing (unrelated to the offer feature) post-verify archive gate exactly as before. `Receipt` was added **additively** instead. This was the only way to satisfy the coordinator's *functional* goal (below) without a further blast-radius risk on top of an already ~600-line slice: removing `Binding`/`BindingRevision` outright requires 6.6+6.7 landing in the SAME slice (review_gate.go's evaluation logic and every reader of the removed fields must be rewired atomically, or the package does not compile) — i.e. 6.5-6.7 are not independently separable once removal is on the table, only once addition is. **6.7's collapse is recommended as its own dedicated slice ("S5c")** with the same characterize-first rigor as this one.
  - **Coordinator's functional goal — CLOSED THIS SLICE**: `OfferReviewAfterVerify`'s enabled-case `Available:false` is no longer a hollow placeholder. `OfferRequest` gained a `Receipt *SDDReceiptRef` field; `review_door.go`'s `reviewOfferForVerify` reads its own runtime ledger's `RuntimeStatus.Receipt` for the change and passes it through (package-boundary constraint: `reviewtransaction` cannot import `sddstatus`, which already imports `reviewtransaction`, so the lookup happens at the `sddstatus`-side caller, not inside `OfferReviewAfterVerify` itself). The real decision: no receipt yet governs the candidate → `Available:true` (genuine invitation); an existing receipt that `ValidateSDDReceiptRef` still resolves `GateAllow` → `Available:false` (already governed, nothing to offer). Proven with a real approved-and-verified compact-authority fixture (`TestOfferReviewAfterVerifyReceiptStillGoverningReportsUnavailable`) and its inverse (`TestOfferReviewAfterVerifyReceiptNotAllowReportsAvailable`); `receipt_ref.go`'s `ValidateSDDReceiptRef`/`receiptRefHash` are now genuinely reachable from `./cmd/gentle-ai` (confirmed by `scripts/deadcode-ratchet.sh` itself reporting them "now reachable").
  - **S5c investigation (2026-08-03), BLOCKED before any code written — worktree left clean at 2aafdfe8, no new branch, no RED test**: a dedicated characterize-first pass traced every non-test reader AND writer of `RuntimeStatus.Binding`/`BindingRevision` (`rg` across `internal/sddstatus/*.go`, `internal/cli/*.go`) and found the literal instruction ("removing RuntimeStatus.BindingRevision/Binding in the same change") is **not compile-safe as a field deletion**: `internal/cli/review_facade.go:1306` calls `sddstatus.BindApprovedReview` as the live, still-scheduled-for-production `gentle-ai review bind-sdd` command — it reads `status.Binding` (review_binding.go:82/85) from `runtimeStore.bindPreparedReview`'s return value. Nothing in tasks.md (S5c, S6, or S7's task 8.1) schedules `bind-sdd`'s retirement; task 8.1 deletes only `review_binding.go`'s writer *functions*, never `review_facade.go`'s call site or the `bindPreparedReview`/`applyRuntimeBindingEvent`/`RuntimeStatus.Binding` plumbing in `runtime_ledger.go`. Deleting the struct field would break a live CLI command outside this wave's own destructive-step scope.
  - Deeper blast radius than S5b's own list, found by tracing actual callers (not just names): `validateRuntimeBoundCandidate` is called from `runtime_ledger.go:564` inside `Finish`'s remediation-successor CAS (not only `status.go:785`); `runtimeSelfSuccessorAvailable`/`runtimeStrandedSuccessor` are called from `runtime_ledger.go:653-654`, also inside `Finish`'s remediation-successor branch; `runtime_compact.go`'s `Settle()` (lines 158-164) reads `status.Binding.Revision`/`.Lineage` to populate `FinishAttemptRequest.ExpectedBindingRevision`/`SuccessorLineageID` for that same CAS. This is one coupled subsystem — the runtime's automatic remediation-successor promotion — not a set of independent "readers" that can be pointed at `ValidateSDDReceiptRef` one at a time: `ExpectedBindingRevision` is a CAS input the writer side (`applyRuntimeBindingEvent`) still needs to accept a *new* approved successor binding, and `SDDReceiptRef`'s design (decision 1, exactly two fields, no `Revision`) has no defined analogue for "the successor lineage a remediation attempt just bound" — `receiptRefDigest` exists as a CAS anchor for `recordPreparedReceipt` but nothing in design.md assigns it a role in the `Finish`/remediation-successor promotion path.
  - **Recommendation for the coordinator**: either (a) re-scope 6.6/6.7 to *only* rewire the archive-gate evaluation path (`status.go`'s two `bindingPresent`→`resolveCompactRemediationAuthority`/`resolveReviewAuthority` call sites, `applyReviewGateEvaluation`) onto `RuntimeStatus.Receipt`/`ValidateSDDReceiptRef`, explicitly leaving `RuntimeStatus.Binding`/`BindingRevision` fields, `BindApprovedReview`/`bind-sdd`, and the entire remediation-successor CAS subsystem (`Finish`, `validateRuntimeBoundCandidate`, `runtimeSelfSuccessorAvailable`, `runtimeStrandedSuccessor`, `Settle()`) untouched and out of scope for this wave — matching what task 8.1 already implies (it never lists `runtime_ledger.go` or `review_facade.go` symbols); or (b) explicitly design a Receipt-based (or hybrid) replacement shape for the remediation-successor CAS before attempting field removal, as its own dedicated design amendment, since none exists today. Silently doing (a) without an explicit coordinator confirmation was judged too large a design decision to make unilaterally in an apply batch — recorded here rather than resolved by omission, per the "never silently resolve" rule.
- [x] 6.8 GREEN: 6.1, 6.2 pass (`SDDReceiptRef`/`parseLegacyBinding`, scoped to what 6.1-6.4 actually built).
- [x] 6.9 RED (threat matrix — Git repository selection): relative `--cwd`, linked worktree, symlinked common dir, foreign-repo refusal. `TestValidateSDDReceiptRefGitRepositorySelection`, 4 subtests, all against the real `SnapshotBuilder.ResolveRepositoryRoot`/`git worktree add`/`os.Symlink` machinery (no mocks).
- [x] 6.10 GREEN: 6.9 via existing `SnapshotBuilder.ResolveRepositoryRoot`; `ValidateSDDReceiptRef` returns before any compact-store read when repository resolution itself fails.
- [x] 6.11 `scripts/deadcode-ratchet.sh --update` for `receipt_ref.go`/`legacy_binding_read.go` exports. Net +4 unwired entries (`ValidateSDDReceiptRef`, `receiptRefHash`, `strictDecodeSDDReceiptRef`, `parseLegacyBinding`) — honest, matching the accepted Wave 3 `review_offer.go` "ship the shape, baseline the gap" precedent; wired once 6.5-6.7 land.

## Phase 7 (S6): Targeted Re-Verify (Three Branches)

- [x] 7.1 **(re-scoped per the coordinator's "targeted re-verify call site" amendment, 2026-08-03)** RED: empty-intersection branch — correction changed paths ∩ verify evidence scope is empty ⇒ `Mode: targeted`, re-run the objective's evidence goal. Proven at the pure-function level: `TestClassifyTargetedReVerifyBranches/7.1_empty_intersection_->_targeted` (`review_reverify_test.go`), genuine compile-fail RED (`undefined: classifyTargetedReVerify` etc.) before `review_reverify.go` existed.
- [x] 7.2 RED: unprovable/non-derivable scoping branch — changed-path set not reliably derivable ⇒ FULL re-verify of the objective's evidence goal (distinct from 7.1, never conflated — proven by asserting the reason string differs). `TestClassifyTargetedReVerifyBranches/7.2_not_reliably_derivable_->_full` + `TestDeriveCorrectionEvidenceBranches` (both branches: no path data at all, and a correction recorded with genuinely no `Snapshot.Paths`). **Investigation finding, matching the amendment's own anticipation**: the governing terminal receipt (`SDDReceiptRef`/`CompactReceipt`) carries no correction-path data at all (only an opaque `FixDeltaHash`) — confirmed by reading `CompactReceipt`'s struct definition. `deriveCorrectionEvidence` reads the fuller `CompactState.CorrectionAttempts[last].Snapshot.Paths` instead (loaded the same way `resolveCompactRemediationAuthority`'s sibling code already loads it), which DOES carry real path data when present — branch 7.2 fires only when that's genuinely empty, not unconditionally. Not extending the receipt schema, per the amendment; noted for Wave 5/7 in design.md's Open Questions.
- [x] 7.3 RED: empty-index/unborn-HEAD branch ⇒ fail closed (no block emitted; native runtime error routing already owns fail-closed reporting elsewhere). `TestClassifyTargetedReVerifyBranches/7.3_fail_closed` + `TestDeriveCorrectionEvidenceBranches`'s unborn-HEAD case. Real signal used: the correction's own captured `Snapshot.UnbornHead` field (set at correction-capture time by the review's own `SnapshotBuilder`, already a real, existing field — `targetedValidationFixFromSnapshot` sets it from `live.UnbornHead`).
- [x] 7.4 (partial, matching the coordinator's explicit "S6 (7.1-7.3)" batch scope) Implemented the three-branch scoping logic (`classifyTargetedReVerify`) and wired it as `Status.ReVerify` via `applyTargetedReVerifyRouting`, called symmetrically from `Resolve()`/`resolveEngramStatus()` (mirroring `applyReviewOfferRouting`'s own call-site shape, per the amendment). **NOT done, DEFERRED to Wave 5 (corrective verify cycle 3, 2026-08-03, CRITICAL-A)**: "record a new `RuntimeAttempt` using the existing `RemediatesEvidenceRevision` field" and "archive does not proceed until that re-verify passes". A second corrective cycle (`03c07581`) attempted this and introduced a livelocking, unsatisfiable archive gate: `applyTargetedReVerifyRouting` re-stamped the demanded evidence revision with the CURRENT verify-report revision on every `Resolve()`, so a compliant re-verify re-labeled the demand instead of clearing it, and the blocked reason named `gentle-ai sdd-attempt finish --remediates-evidence-revision <rev>`, which the CLI refuses to run standalone: `finish` requires 8 flags, and `--remediates-evidence-revision` additionally requires `--expected-binding-revision` and `--successor-lineage` TOGETHER — a full review round trip, defeating the "run a cheap targeted re-verify" scenario this mechanism exists for. The cycle-2 apply-progress claim "no new writer needed" was wrong. A third corrective cycle removed the gate entirely (`review_reverify.go` reverted to its original purely-additive/informational shape — `Status.ReVerify` routes and classifies, never mutates `Dependencies`/`NextRecommended`) rather than ship a livelock or rush an undertested decoupled writer into a security-sensitive ledger-validation path. See design.md's "Amendment (corrective verify cycle 3): re-verify archive-gating deferred to Wave 5" and the matching spec amendment for the full rationale and the shape a compliant Wave-5 writer needs.
- [x] 7.5 GREEN: 7.1–7.3 pass (pure-function level, thorough table-driven coverage) plus two real end-to-end `Resolve()` tests: `TestResolveOmitsReVerifyBlockWithoutAnyCorrection` (structural absence, real on-disk approved compact authority, PASSING) and one investigated-but-not-pursued gap recorded explicitly in `review_reverify_test.go` — a fully protocol-valid on-disk correction fixture (real `BeginCorrection`+git fix-diff+`FixDeltaHash`+`CompleteCorrectionVerification` round trip) proved to need substantially more machinery than this slice's budget once `reviewtransaction`'s own `validateCompactCorrection` cross-checks were discovered empirically; not silently dropped, see the test file's own comment for the full investigation.
- [ ] 7.6 RED (threat matrix — Commit state, remaining cases): staged, `commit -a`. **NOT attempted** — out of the coordinator's stated "S6 (7.1-7.3)" batch scope, and this slice's implementation never performs a live Git diff for correction-path derivation (it reads already-persisted `CorrectionAttempts` data), so "staged"/"commit -a" at STATUS-READ time has no bearing on this code path as currently scoped — would only become relevant if a future wave adds live correction-path derivation.
- [ ] 7.7 GREEN: 7.6 passes. **NOT attempted**, depends on 7.6.
- [x] 7.8 `scripts/deadcode-ratchet.sh --update` for re-verify branch exports. Ran (check mode): "no new unreachable functions" — everything added (`ReVerifyBlock`, `classifyTargetedReVerify`, `deriveCorrectionEvidence`, `intersectPaths`, `verifyEvidenceScope`, `applyTargetedReVerifyRouting`) is genuinely reachable from `Resolve()`/`resolveEngramStatus()`; no baseline update needed.

**Design interpretation recorded explicitly (not a literal design.md field name)**: "verify evidence scope" is approximated as the compact authority's `GenesisPaths` narrowed to the `openspec/changes/<changeName>/` prefix (the same prefix `compactAuthorityPathsBound` already uses). Investigated and rejected: using the FULL `GenesisPaths` as evidence scope, because `reviewtransaction`'s own state machine validates every correction attempt's paths as a subset of `GenesisPaths` (confirmed empirically), which would make branch 7.1's empty intersection structurally unreachable (a subset's intersection with its own superset is never empty unless the subset itself is empty — already 7.2's territory). See `review_reverify.go`'s top-level doc comment for the full investigation.

## Phase 8 (S7): Mirror Deletion + Asset Updates (lands LAST — only destructive step)

- [x] 8.1 **Re-scoped per the coordinator's decision-1 amendment (2026-08-03)**: literal "delete the full writer half" did NOT happen — `BindApprovedReview`, `prepareApproved*Binding`, `bindingBytes`/`bindingDigest`/`bindingPath`, `verifyBindingLedger`, `runtimeSelfSuccessorAvailable`, `RuntimeStrandedSuccessor` all stay (Wave 7 territory; `review bind-sdd`/the remediation-successor CAS still need them). Instead: the deadcode ratchet was run as the arbiter immediately after S5c' landed, and it named exactly two genuinely orphaned symbols — `bindingExists` and `validateBoundReview` (`review_binding.go`) — both deleted, along with the now-unused `bindingFinalAuthorizationHook` test-hook var (its only caller was the deleted `validateBoundReview`). Everything else in the original 8.1 list is KEPT; see the Wave 7 deletion-inventory Engram note (`sdd/rdd-root-simplification-wave7/deletion-inventory-w4-contributions`) for the one-line-each record of what stays and why.
- [x] 8.2 (re-scoped: no separate "BindingWriterRemoved guard" — the writer half was NOT removed, so that guard's premise no longer applies) Instead: `TestBindingExistsRequiresAParsedNativeBinding` was ported to `TestResolveGoverningReceiptRefRequiresAParsedNativeBinding` (`review_gate_receipt_test.go`), preserving its two properties — attempt-only runtime HEAD is not governance, corrupt native runtime HEAD fails closed — against the new `resolveGoverningReceiptRef` symbol. `TestValidateBoundReviewFailsClosedWhenFinalGateChanges`/`TestValidateBoundReviewFailsClosedForFinalAuthorityArtifacts` were deleted outright (they tested `validateBoundReview`'s TOCTOU double-read hardening, already recorded in S5c' as a deliberate, non-carried-forward scope reduction — nothing to port). `TestBoundReviewBindsLedgerAcceptsLegacyEmptyAndRejectsForgedHash` (`review_binding_ledger_test.go`) was NOT deleted — its 3 `validateBoundReview` calls were rewired to a new `validateCurrentBoundCandidate` helper that reloads the binding and calls `validateRuntimeBoundCandidate` directly, preserving genuine coverage of `boundGateContextMatches`'s empty-ledger-hash backward-compat exemption, which remains load-bearing for the (untouched) remediation-successor CAS.
- [x] 8.3 GREEN: full `internal/sddstatus` suite green after the 8.1/8.2 deletions and rewires; full root module (61 packages) green; bench module green; deadcode ratchet net -2 (241→239, exactly reversing S5c''s +2 orphaning — confirms nothing else needed either symbol).
- [x] 8.4 **Investigated (2026-08-03), NOTHING thinned — recorded explicitly, not silently skipped.** Read `internal/assets/opencode/plugins/review-result-artifacts.ts` (504 lines) in full against the CURRENT (post-re-scope) shape. Finding: the file's `ReviewBinding` TypeScript type (line 10) and the session-scoped admission-recovery machinery (`admissionRecoveryKey`, `claimAdmissionRecovery`, `clearAdmissionRecovery`, `MAX_ADMISSION_RECOVERIES_PER_SESSION`) are **entirely unrelated** to the Go-side `sddstatus.ReviewBinding`/`bind-sdd` this wave's S5c'/S7 slices touched — same name, structurally unconnected concern. The TS `ReviewBinding` describes the `GENTLE_AI_REVIEW_BINDING` capture-task binding (`lineage`/`target`/`lens`/`order`/`revision`/`repository_context`/`subject_hash`) used for `review capture-result`/`review preserve-result` CLI invocations; it is used pervasively (14 call sites across nearly every function in the file) and has no reference anywhere to `bind-sdd`, `bindingExists`, or `validateBoundReview` (confirmed: `rg "bind-sdd|bindingExists|validateBoundReview|ReviewBinding" internal/assets/` finds zero cross-references). The admission-recovery budget is a retry-loop safety property for review-capture admission REJECTIONS (finding-location diagnostics), also unrelated to SDD archive-gate governance. Since S5c'/S7 only deleted `bindingExists`/`validateBoundReview` (neither referenced here) and explicitly left `Binding`/`bind-sdd` untouched (the amendment's own decision), nothing in this file became dead as a consequence of this wave's actual changes. "Consume opaque transitions only" (the original task text) describes a conditional future state — the plugin's local session-scoped recovery bookkeeping could only be REMOVED once a provider-side replacement exists to own that state instead, which is exactly task 8.5's open question, not something this wave built.
- [x] 8.5 **Decision recorded (coordinator ruling, 2026-08-03): DEFERRED, not built this wave.** The relaunch-bound-loss provider-side replacement is deferred — either to Wave 5's entry (the gates-cutover work owns the admission flow) or to Wave 7 (if the plugin's whole legacy consumption retires there instead). Not silently dropped: recorded here, in design.md's Open Questions (already amended), and in the Wave 7 deletion-inventory Engram note (`sdd/rdd-root-simplification-wave7/deletion-inventory-w4-contributions`) with the exact loss being deferred (session-scoped `Map<string, Set<string>>` admission-recovery bookkeeping in `review-result-artifacts.ts`, bounded by `MAX_ADMISSION_RECOVERY_SESSIONS`/`MAX_ADMISSION_RECOVERIES_PER_SESSION`, currently the ONLY implementation of that relaunch-bound safety property).
- [x] 8.6 Updated `internal/assets/claude/commands/sdd-apply.md` and `internal/assets/opencode/commands/sdd-apply.md`: their "POST-APPLY REVIEW ROUTING" sections claimed `nextRecommended: review` triggers negotiated review routing — confirmed stale by reading `resolveNextRecommended` (`status.go`): that function never returns the literal string `"review"` (S3 already removed `applyPreVerifyReviewRouting`; the only review-adjacent values are `resolve-review` and `remediate`). Rewrote both sections as "REVIEW ROUTING (post-verify, not post-apply)": apply's own `nextRecommended` proceeds straight to `verify`; the review offer is a post-verify decision keyed off the `reviewOffer` block (pointing to `sdd-verify.md`'s own post-verify offer prose, already updated in S3b), and the apply executor never launches review. `skills/_shared/review-ledger-contract.md` **investigated, NO changes made**: its only "post-apply" reference (line 40) names one of the review provider's own unrelated delivery-gate identifiers (`post-apply`/`pre-commit`/`pre-push`/`pre-pr`/`release` — Git-hook-triggered receipt validation, a different "post-apply" concept than SDD's apply phase); it contains no `nextRecommended: review`-style stale routing claim to correct.
- [x] 8.7 Regenerated `testdata/golden/sdd-claude-cmd-sdd-apply.golden` and `testdata/golden/sdd-opencode-cmd-sdd-apply.golden` (`go test ./internal/components/ -run 'TestGoldenSDD_Claude|TestGoldenSDD_OpenCode' -update`). Byte-diff verified scoped to exactly the "POST-APPLY REVIEW ROUTING" → "REVIEW ROUTING (post-verify, not post-apply)" section (S1's protocol). Rerun without `-update`: full `internal/components -run Golden` suite green (all SDD/persona/engram/skills/combined goldens).
- [x] 8.8 `scripts/deadcode-ratchet.sh --update` for the 8.1/8.2 deletions — done as part of 8.3 (net -2, 241→239). Asset-change ratchet coverage still pending 8.4/8.6.
