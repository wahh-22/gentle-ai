# Tasks: RDD Root Simplification — Wave 6 (Descendant Closure)

Hybrid store: also Engram `sdd/rdd-root-simplification-wave6/tasks`.

## Gate

- [x] 0.0 Verify Waves 3, 4, 5 are merged into tracker `feature/rdd-root-simplification` before opening PR1. Chain strategy is feature-branch-chain: PR1 targets the tracker branch, PR2 targets PR1's branch, PR3→PR2, PR4→PR3, PR5→PR4. Only the tracker merges to `main`. Retarget/rebase if a child diff shows a previous slice's changes. Verified: tracker tip `bb3c22a9` (fetched, unchanged during S1) carries wave3 merges (#2309-#2314), wave4 merges (#2327-#2339, #2354), and wave5 merges (#2370-#2383) in `git log --oneline --merges`. Wave 6's own SDD artifacts (this file, proposal.md, design.md, specs/) were already committed to the tracker line via `3e42f58b` — PR0 needs no further action.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~600 (PR1) + ~900 (PR2) + ~700 (PR3) + ~600 (PR4) + ~800 (PR5) ≈ 3600 total |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR0 (SDD artifacts, no code) → PR1 (S1) → PR2 (S2) → PR3 (S3) → PR4 (S4/D7) → PR5 (ds09+ journeys) |
| Delivery strategy | auto-chain |
| Chain strategy | feature-branch-chain |
| Per-PR budget | 1000 lines/PR (session override; repo CI default 400 + `size:exception`) |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

PR2 and, before splitting, the design's combined S4 sit near the 1000-line ceiling — this task set splits the design's S4 (negotiated transition + ds09-ds12, ~900) into PR4 (D7 only) and PR5 (journeys only) to keep both slices clear of the ceiling and to preserve D7 as the sole, cleanly droppable slice on overrun.

### Suggested Work Units

| Unit | Goal | PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 0 | SDD artifacts only (proposal/spec/design/tasks) | PR0 | N/A — no code | N/A — docs only | Revert commit; no runtime state |
| 1 | Topological ordering + N≥1 admission | PR1 | `go test ./internal/reviewtransaction/... -run TestAuthorityDispositionClosure -count=1` | `go run ./bench run --axis damaged-store --journey ds06,ds08` (byte-stability regression) | Revert `authority_disposition_plan.go`/`authority_disposition_execute.go` hunks; N=1 path untouched |
| 2 | Ordered N-node transaction, CAS-all-N, manifest | PR2 | `go test ./internal/reviewtransaction/... -run TestAuthorityDispositionExecute -count=1` | `go run ./bench run --axis damaged-store --journey ds09,ds10` (once written in PR5; until then unit test only) | Revert `lockedAuthorityDispositionMutation` loop + `compact_reclaim.go` manifest writer; admission stays N=1-only if PR1 also reverted |
| 3 | Forward-only resume + crash-position tests | PR3 | `go test ./internal/reviewtransaction/... -run TestAuthorityDispositionResume -count=1` | `compactReclaimPhaseHook` crash injection (in-process, N/A external harness) | Revert resume branch; PR2's non-resumed loop still functions, only replay is lost |
| 4 | Negotiated transition (D7) | PR4 | `go test ./internal/cli/... -run TestReviewNextTransition -count=1` | `gentle-ai review status --next-transition` against a repairable-classified fixture | Revert `reviewRepairTransition` disposition branch + `compact_inspect.go` exit restore; raw flag triad still works |
| 5 | ds09+ bench journeys (exit evidence) | PR5 | `go test ./internal/reviewtransaction/... -count=1` (regression) | `go run ./bench run --axis damaged-store --journey ds09,ds10,ds11,ds12` | Revert journey additions to `bench/axis_damaged_store.go`; ds01-ds08 untouched |

## Phase 1 (PR1 — S1): Ordering + Admission — COMPLETE (branch `feat/rdd-wave6-s1-descendant-closure-ordering`, commit `8ee2e2ef`)

- [x] 1.1 RED (assumption check, FIRST — blocks all other S1 work): in `authority_disposition_plan_test.go`, build a real multi-chain `report.Edges` fixture (≥2 chains, ≥3 nodes) and assert `authorityDispositionClosure`'s BFS `children` map, built from `PredecessorLineageID→SuccessorLineageID`, actually yields the claimed multi-chain closure. This validates the design risk-list assumption against a real fixture before the closure loop is written. **Verdict: GO** — `TestAuthorityDispositionClosureMultiChainAssumption` passed on first run against a real 3-node/2-chain fixture; the BFS `children` map correctly derives multi-hop closures with no over-collection.
- [x] 1.2 If 1.1 fails: STOP, escalate — the multi-chain assumption is load-bearing for S2/S3/S5; do not patch around it ad hoc. (N/A — 1.1 passed.)
- [x] 1.3 RED: descendant-first, seed-last ordering — N=1 identity-of-old-sort case and N≥2 multi-chain case (spec `rdd-authority-disposition-plan` / "Ordering is descendant-first, seed-last"). `TestAuthorityDispositionClosureDescendantFirstSeedLastOrdering` — genuine RED confirmed (N≥2 and tie-break subtests failed against the pre-change lexicographic sort).
- [x] 1.4 GREEN: replace the `slices.SortFunc` tail in `authorityDispositionClosure` (`authority_disposition_plan.go:162`) with a topological descendant-first emit over the existing BFS `children` map; ties broken lexicographically. Implemented as a post-order DFS with lexicographically-sorted children per node.
- [x] 1.5 RED: `plan_digest` byte-stability for N=1 (ds06/ds08 goldens unchanged). `TestAuthorityDispositionPlanDigestN1ByteStability` pins the N=1 digest; empirically confirmed byte-identical against both the pre- and post-Wave-6 `authorityDispositionClosure` (verified via `git stash` on the production file only). ds06/ds08 bench journeys re-run against a fresh binary post-change: both `completed`.
- [x] 1.6 RED: admission — N=1 admitted, N≥2 classified admitted, unknown/mixed/ambiguous refused (spec `rdd-closure-disposition-execution` / "N-Node Admission for Closed Anomaly Classes", both scenarios). `TestAuthorityDispositionClosureAdmission` — RED confirmed via compile failure (`admitClosureDisposition` undefined) before 1.7's GREEN.
- [x] 1.7 GREEN: relax `admitLeafDisposition` (`authority_disposition_execute.go:34`) to `len(SeedSet)==1 && len(Closure)>=1 && Closure[len-1]==SeedSet[0]`; rename export `AdmitAuthorityDispositionLeaf`→`AdmitAuthorityDispositionClosure`; update its 5 call sites (`authority_disposition_execute.go`, `compact_inspect.go`). Also updated the 3 external call sites (`review_repair.go` x2, `compact_damaged_store_exit_test.go` x1) using the exported name.
- [x] 1.8 RED: leaf regression — `rdd-leaf-disposition-execution` / "Single-node closure is admitted" scenario passes (`TestAuthorityDispositionExecuteAdmitsSingleNodeClosure`, unchanged, still green); the multi-node #2014-naming refusal scenario is deleted, not skipped (`TestAuthorityDispositionExecuteRefusesMultiNodeClosure` removed). The separate pre-existing `TestAuthorityDispositionExecuteRefusalNamesDiagnosisAndEscalationArtifact` (#1656-naming, not named for deletion) is kept green: its fixture's malformed closure order (seed not last) now refuses via the new `errAuthorityDispositionShape`, whose message still names #1656 with no delivery-date commitment.
- [x] 1.9 Threat matrix RED (Git repository selection, applicable): relative/absolute/foreign-repo `--cwd` still refuse or bind correctly through `ResolveRepositoryRoot`/`authorityRepairRoot` under the renamed export. New `TestAuthorityDispositionClosureGitRepositorySelectionThreatMatrix` (3 subtests) — all pass.
- [x] 1.10 REFACTOR/deletion (D9, scoped): delete `errAuthorityDispositionCardinality`, its `#1656`/`#2014`/"a future wave" text, and `TestAuthorityDispositionExecuteRefusesMultiNodeClosure`. Do not touch `ReconcileInvalidRecoveryEdge` — see Deletion Deferral below. Done as part of 1.7's rename (the old error var/message are replaced, not kept alongside a new one).
- [x] 1.11 `go test ./... -count=1`; `go run ./bench run --axis damaged-store`; deadcode ratchet; write refusal-resolution notes for every refusal text changed or removed. Root suite: all packages `ok` (`internal/reviewtransaction` 126.1s, `internal/cli` 169.2s, full repo run). Bench module: `go test ./...` ok; `ds06`/`ds08` against a fresh binary: both `completed`. `scripts/deadcode-ratchet.sh`: "no new unreachable functions" (no `--update` needed). Refusal-resolution notes: see commit message.

## Phase 2 (PR2 — S2): Ordered N-Node Transaction — COMPLETE (branch `feat/rdd-wave6-s2-ordered-transaction`, base `feat/rdd-wave6-s1-descendant-closure-ordering` @ `41a471ff`)

- [x] 2.1 RED: crash after N-1 of N nodes — retained graph classifies cleanly, only the seed unmoved (spec: "Crash after N-1 of N nodes leaves a valid graph"). `TestAuthorityDispositionExecuteCrashAfterNMinus1LeavesOnlySeedUnmoved` — real 2-node closure, injected failure at the descendant's `compactReclaimPhaseCommitted` hook; asserts `report.Complete` (no dangling reference), NOT `report.Valid` (the seed's own still-unfixed forged edge legitimately stays invalid until the seed itself, last in order, also commits).
- [x] 2.2 GREEN: `lockedAuthorityDispositionMutation` (`authority_disposition_execute.go`) becomes an ordered loop over `plan.Closure`, reusing `quarantineCompactStoreEntry` per node unchanged. Order-inversion mutation proof: `TestAuthorityDispositionExecuteOrderedTransactionFollowsClosureOrder` records every `compactReclaimPhasePrepared` hook's LineageID and asserts the sequence equals `plan.Closure` exactly (3-node closure).
- [x] 2.3 RED: CAS-all-N — all `ExpectedRevisions` checked before the first move; drift on any non-seed member refuses pre-move, naming exactly which member. `TestAuthorityDispositionExecuteCASAllNRefusesNamingDriftedMember` corrupts the submitted plan's `ExpectedRevisions[child]` post-authorization (doesn't touch the authorization pre-image or the cached `plan.PlanDigest`, so it exercises CAS-all-N specifically, not the pre-existing digest-mismatch refusal).
- [x] 2.4 GREEN: pre-loop CAS validation across the full closure, positioned after authorization validation, before the first `quarantineCompactStoreEntry` call; refusal names the exact closure member (`%q`).
- [x] 2.5 RED: partial closure never reports success (spec: "Atomic Visibility With Forward-Only Convergence"). `TestAuthorityDispositionExecutePartialClosureReportsNoSuccess` — no `closure-manifest.json` exists anywhere in the quarantine root after an interruption; the descendant's own `reclaim-record.json` still shows `committed` (nothing already-committed is undone).
- [x] 2.6 GREEN: gate `readBackAuthorityDisposition` behind last-node commit — it is called exactly once, after the ordered loop returns, using the seed's (last-processed) record.
- [x] 2.7 RED: closure-manifest schema — `closure-manifest.json` written inside the seed node's quarantine dir, ordered closure + digest (D5, forensic-only). `TestAuthorityDispositionClosureManifestWrittenInSeedQuarantineDir`.
- [x] 2.8 GREEN: manifest writer (`writeAuthorityDispositionClosureManifest`, `AuthorityDispositionClosureManifest`, `AuthorityDispositionClosureManifestSchema`) added to `compact_reclaim.go` beside `persistCompactReclaimRecord`; called once after the ordered loop completes, before returning (design.md's Data Flow: manifest step follows the loop, before readback — never a lifecycle dependency, never read by resume logic).
- [x] 2.9 RED: unrelated-lineage byte-identical assertion, mirroring the `requireDispositionWitnessBytesUnchanged` pattern. `TestAuthorityDispositionExecuteUnrelatedLineageByteIdenticalAcrossClosure` (witness lineage created BEFORE plan derivation, so it doesn't trip the unrelated digest-staleness guard).
- [x] Over-collection guard widening (design D6, beyond the numbered list but required by the coordinator's S2 scope): `readBackAuthorityDisposition`'s dangling-reference check widened from `record.LineageID` alone to every `record.AuthorityDisposition.Closure` member via new `authorityDispositionClosureMembers`. `TestReadBackAuthorityDispositionRefusesOnAnyClosureMemberReference` exercises the widened check directly against a synthetic non-seed-member-still-referenced fixture.
- [x] 2.10 `go test ./... -count=1` (root, all packages ok, `internal/reviewtransaction` 121.9s); `go run ./bench run --axis damaged-store --only ds06-...,ds08-...` against a fresh binary (both `completed`); `scripts/deadcode-ratchet.sh` — "no new unreachable functions"; gofmt/vet clean. Refusal-resolution notes: CAS-all-N refusal names the exact closure member (`expected revision for closure member %q drifted`); readback over-collection refusal names the exact still-referenced closure member. Tracker advanced `bb3c22a9`→`0d179601` during this slice (docs-only, `openspec/changes/...` and `openspec/specs/...`, zero overlap with `internal/reviewtransaction`) — no rebase required per the rebase contract.

## Phase 3 (PR3 — S3): Forward-Only Resume — COMPLETE (branch `feat/rdd-wave6-s3-forward-resume`, base S2 @ `8768b7cf`, commit pending — see apply-progress)

- [x] 3.1 RED: exact replay resumes without a double move — committed skipped, prepared completes via `residue/` (spec scenario). `TestAuthorityDispositionResumeSkipsCommittedAndFinishesPrepared` (3-node closure) + `TestAuthorityDispositionResumeNeverReprocessesCommittedNode` (skip-committed mutation proof: the committed node's source path is never resurrected by a resume).
- [x] 3.2 GREEN: `discoverAuthorityDispositionRecord`/`resumeAuthorityDispositionRecord` invoked per node inside the ordered loop (reused as-is — already lineage-generic despite its `seed`-named parameter). New `authorityDispositionClosureIsFresh` decides, using ONLY `plan.Closure[0]` (disposition always starts with the deepest descendant), whether the whole re-derive/CAS-all-N/authorize gate runs (genuinely fresh) or is skipped entirely (any prior progress) — re-running that gate against a store that has legitimately shrunk as prior members were quarantined away would itself be a forbidden narrowing re-derivation.
- [x] 3.3 RED: digest mismatch or non-re-deriving graph refuses, names the manifest path, escalates — no narrowing re-derivation attempted. `TestAuthorityDispositionResumeDigestMismatchRefusesNamingQuarantinePath`. New `authorityDispositionResumeDigestMismatchRefusal` names the seed's `closure-manifest.json` when a completed run exists under the mismatched digest, else the specific member's own quarantine directory, else a generic escalation — never a narrower re-derived plan. All 4 return sites carry `refusal:by-design human-authority` annotations (caught by `TestEveryProductionRefusalNamesResolutionOrDeclaresByDesign` in `internal/cli` — fixed mid-slice).
- [x] 3.4 GREEN: digest/re-derivation drift check ahead of resume — `authorityDispositionClosureIsFresh` is called before any validation; the ordered loop's per-node `discoverAuthorityDispositionRecord` (found = skip/resume; not-found + missing v2/ entry = digest-mismatch refusal; not-found + live v2/ entry = fresh quarantine) replaces S2's always-fresh loop. Mutation proof: `TestAuthorityDispositionResumeStalePlanRefusesNotProceeds` — a stale `PlanDigest` on an otherwise-fresh graph refuses via the pre-existing digest-mismatch check and moves nothing.
- [x] 3.5 RED integration: crash-position matrix via `compactReclaimPhaseHook` at every ordered position of a 3-node closure. `TestAuthorityDispositionResumeCrashPositionMatrix` — 6 positions (prepared + committed phases × grandchild/child/seed), each: interrupt, resume, and byte-compare the final `residue/` content per closure member against an uninterrupted control run of the identical fixture (same suffix/lineage IDs), plus retained-graph revalidation and unrelated-witness-lineage byte-identity per position (the over-collection guard's runtime half).
- [x] 3.6 GREEN: confirmed loop coverage is sufficient at every position — no production code beyond 3.2/3.4's `authorityDispositionClosureIsFresh` + per-node discovery + `authorityDispositionResumeDigestMismatchRefusal` was needed; `resumeAuthorityDispositionRecord`'s existing ambiguous-residue discriminator extends to non-seed members for free (`TestAuthorityDispositionResumeAmbiguousResidueRefusesPerNode`).
- [x] 3.7 `go test ./... -count=1` (root, all packages ok, `internal/reviewtransaction` 125.7s, `internal/cli` 172.7s — including the refusal-resolution ratchet); `go run ./bench run --axis damaged-store --only ds06-...,ds08-...` against a fresh binary (both `completed`); `scripts/deadcode-ratchet.sh`: "no new unreachable functions"; gofmt/vet clean. Refusal-resolution notes: the digest-mismatch refusal names either `closure-manifest.json` (completed-under-a-different-digest case) or the specific member's quarantine directory (still-incomplete case), annotated `refusal:by-design human-authority` at all 4 return sites.

## Phase 4 (PR4 — S4/D7): Negotiated Transition — COMPLETE, not dropped (branch `feat/rdd-wave6-s4-negotiated-route`, base S3 @ `d17b088d`, commit pending — see apply-progress)

Budget check before this slice: chain sat at ~1733 authored lines across S1-S3, well under pressure — S4 did NOT need dropping (4.7's overrun guard was not triggered).

- [x] 4.1 RED: `next_transition` offers disposition `collect{disposition_authorization}` / `execute{review.repair, --plan-digest --inventory-revision --actor --reason --authorization}` for a repairable-classified graph with an authorized closure plan. `TestReviewNextTransitionDispositionCollectThenExecute` (collect-then-execute, both subtests) + `TestReviewNextTransitionClassifiedRepairTakesPriorityOverDisposition` + `TestReviewNextTransitionCorruptedWithNeitherRepairNorDispositionStops` (regression) — all new, since `reviewRepairTransition` had zero covering tests before this slice (design risk note addressed, not shipped blind).
- [x] 4.2 GREEN: new `reviewDispositionTransition` (`review_next_transition.go`, beside `reviewRepairTransition`) serves the SAME `review.repair` operation through collect{}/execute{}, reached only after the classified route above it has already refused (`status.Disposition != nil` check added to the `TargetApplicabilityCorrupted` case, evaluated after `status.Repair`). New `ReviewTargetStatusResult.Disposition *ReviewRepairDispositionProviderInputs` field populated in `review_facade.go` mirroring `review repair --preflight`'s own read-only derivation (empty actor/reason, `AdmitAuthorityDispositionClosure` gate, no propagated derivation refusal). New exported `reviewtransaction.AuthorityDispositionAuthorizationSchema` (alias for the existing unexported schema const) so `internal/cli` can publish the collect{}'s schema without duplicating the literal.
- [x] 4.3 RED: `compactStartInvalidGraphRefusal`'s continuation prose (via `SanctionedCompactRecoveryExits`, the exact seam it renders through) still names `CompactRecoveryEdgeExitRepair` for a closed, NON-PRISTINE content-mismatched-recovery-authorization successor (the #2014 gap neither reconcile nor abandon accepts). `TestCompactStartInvalidGraphRefusalStillNamesRepairExit` — new fixture (`writeNonPristineDispositionRepairFixture`) since the existing CLI helper only builds pristine successors, which abandon claims first.
- [x] 4.4 GREEN: no code change needed — this exit was already correctly wired by Wave 2 Slice S3 and nothing in S1-S3 touched the switch case; 4.3's new test is the regression guard confirming it, closing the "no covering tests" gap rather than restoring already-present behavior.
- [x] 4.5 Threat matrix RED (PR commands, applicable): emitted transition tokens contain no authorization bytes (`"provided"` sentinel) and run verbatim via `reviewTokenizedTransitionArguments`. `TestReviewDispositionTransitionTokensCarryNoAuthorizationBytes` — a distinctive secret authorization string never appears in any argument Value/Token or the rendered Command; every argument carries a non-empty Token (proving it ran through the shared tokenizer).
- [x] 4.6 `go test ./... -count=1` (root all packages ok, `internal/reviewtransaction` 122.3s, `internal/cli` 175.2s including the refusal-resolution ratchet); `go run ./bench run --axis damaged-store --only ds06-...,ds08-...` against a fresh binary (both `completed`); `scripts/deadcode-ratchet.sh`: "no new unreachable functions"; gofmt/vet clean.
- [x] 4.7 Overrun guard: N/A — chain stayed well under pressure (budget check confirmed before this slice), so S4 shipped in full, not deferred.

## Phase 5 (PR5): ds09+ Bench Journeys — Exit Evidence — COMPLETE (branch `feat/rdd-wave6-s5-closure-journeys`, base S4 @ `48a70562`, commit pending — see apply-progress)

Real product constraint discovered while building the fixture: `review recover` refuses "recovery predecessor already has successor" — a lineage can have at most ONE direct successor through the CLI, so a closure with more than one descendant can only ever be a LINEAR chain, never a fork/branch. ds09-ds11 use a 3-node linear chain (seed -> child -> grandchild) instead of a branching tree; this still exercises the multi-hop BFS/DFS closure derivation (S1) and the N=3 ordered transaction (S2) the task names, only the "branching" framing does not exist as a constructible product state.

- [x] 5.1 `bench/axis_damaged_store_closure.go` (new file, appended into `damagedStoreJourneys()` via `closureDispositionJourneys()`): `ds09-multi-chain-closure` — N=3 linear closure (1 damaged seed edge + 2 valid descendant hops), classified and disposed end-to-end, byte-preserving. Fixture (`multiHopClosureFixture`) mints every hop FIRST while the whole graph is valid (write-time validation refuses a new edge once any existing edge is invalid), then damages the seed's edge LAST and re-signs (`realignRecoveryAuthorization`) each descendant's authorization against its predecessor's new post-damage revision so descendant edges stay genuinely valid.
- [x] 5.2 `ds10-cross-lineage-closure` — reachable chain members quarantined, the unrelated witness lineage AND the closure's own top-level predecessor (not itself a closure member) both stay byte-identical (spec: "Cross-lineage closure disposes only reachable nodes").
- [x] 5.3 `ds11-crash-recovery-mid-closure` — authors the first (deepest) closure member as already-committed directly on disk (this axis's established "fixtures author review-state.json directly" convention, extended to a real `quarantineCompactStoreEntry` output shape), then resumes through the REAL `review repair` binary; proves no double move (every closure member ends with exactly one quarantine directory) and a clean retained-graph revalidation. Covers the first (deepest-descendant) position specifically — the unit-level `TestAuthorityDispositionResumeCrashPositionMatrix` (S3) already proves every other position converges by the identical seam.
- [x] 5.4 `ds12-negotiated-transition-route` — `review status --next-transition` disposition collect/execute journey; PR4/S4 shipped in full (budget check confirmed before S4), so ds12 ships too, not deferred.
- [x] 5.5 `go test ./... -count=1` (root all packages ok, `internal/reviewtransaction` 135.6s, `internal/cli` 190.6s including the refusal-resolution ratchet); `go run ./bench run --axis damaged-store --only ds06,...,ds12` (all 7 disposition journeys `completed` together against a fresh binary); deadcode ratchet: "no new unreachable functions". Refusal-resolution notes below.

**Genuine defect found and fixed by ds11/ds12 (not merely disclosed — corrected in this slice):** `RepairAuthorityDisposition`/`repairAuthorityDispositionAtRepo` (the ONLY public entrypoint `review repair` calls) always re-derived the plan FRESH from actor/reason on every call, discarding the caller-supplied `--plan-digest`/`--inventory-revision` entirely. Once ANY closure member is already quarantined (a genuine mid-transaction crash, not just this journey's authored one), fresh re-derivation necessarily produces a smaller, digest-mismatched closure — refusing "review repair leaf authority disposition inputs do not match the current provider-derived plan" for EVERY real crash-then-resume through the CLI, even though `executeAuthorityDisposition`'s own internal resume logic (Slice S3) was already correct. This gap existed only at the public-entrypoint layer — S3's unit tests all call `executeAuthorityDisposition` directly, bypassing it entirely — and was caught ONLY by ds11's journey-level (real-binary) exercise, confirming exactly why the coordinator asked for "the journey-level twin" of the crash matrix.
  - Fix: `RepairAuthorityDisposition`'s signature now accepts `planDigest`/`inventoryRevision` explicitly. New `reconstructAuthorityDispositionPlanForResume` scans the quarantine root for an existing record whose `AuthorityDisposition.PlanDigest` matches, and reconstructs the FULL original plan from that record's own proof (never a narrowing re-derivation) when found; only falls back to fresh derivation (compared against the caller's digest) when no such record exists.
  - `internal/cli/review_repair.go`'s own REDUNDANT pre-check (derive + admit + compare, duplicating what the reviewtransaction package now owns) removed; it now delegates the whole fresh-vs-resume decision to `RepairAuthorityDisposition`.
  - New unit-level regression: `TestRepairAuthorityDispositionResumesThroughPublicEntrypoint` (`internal/reviewtransaction`) — the same crash-then-resume proof as the Go-level matrix, but through the PUBLIC entrypoint specifically, closing the exact gap the unit tests never covered.
  - A second, smaller gap in the SAME area: the generic status-envelope validator (`validateNextTransitionTargets`'s execute-case check) required every "review.repair" execute transition to carry a non-empty `Binding.LineageID`/valid `Binding.Revision` — a shape only classified repair (which has a single candidate lineage) previously satisfied. Fixed by adding `SeedLineageID`/`SeedExpectedRevision` to `ReviewRepairDispositionProviderInputs` (populated ONLY for the `review status` negotiated-route publication, never for `review repair --preflight`, which must never leak lineage identity — `TestReviewRepairPreflightSurfacesAuthorityDispositionPlanForEligibleLeaf`'s pre-existing byte-level assertion caught this distinction immediately when first attempted).

## Deletion Deferral (D9 / Wave 7)

- [x] D.1 PR1 (task 1.10) deletes only `errAuthorityDispositionCardinality`, its `#1656`/`#2014` refusal text, and `TestAuthorityDispositionExecuteRefusesMultiNodeClosure`.
- [x] D.2 Do NOT delete `ReconcileInvalidRecoveryEdge` (`compact_reconcile.go:233`) this wave. It has 4 live consumers confirmed by call-graph: `review_reconcile.go`, `review_reconcile_batch.go`, `compact_batch_reconcile_journal.go`, and bench journeys `ds01`/`ds02`/`ds04`. Record this evidence in the PR1 description; deletion is Wave 7's public-surface retirement with its own consumer-migration evidence, not this wave's admission relaxation. Confirmed untouched — `ReconcileInvalidRecoveryEdge` does not appear in S1's diff.
