# Tasks: Organic DX

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1050-1500 (12 asset diffs, 3 new test files, 2 new registries, defect-report builder, facade seam, e2e scenarios) |
| 400-line budget risk | High |
| Chained PRs recommended | No |
| Suggested split | Single PR #1801 |
| Delivery strategy | exception-ok |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: High

Maintainer accepted `size:exception` for PR #1801 this session; size never stops or splits the chain. Phases below are structured as independently committable work units for audit clarity — **no task commits**; the tree stays uncommitted for maintainer review.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 (Phase 1) | Auto-default assets + anchors | PR #1801 | `go test ./internal/assets/...` | N/A — text-only, no runtime path | revert 12 asset diffs + anchor test |
| 2 (Phase 2) | Visible escalation accounting + gatekeeper-guard resolution | PR #1801 | `go test ./internal/reviewtransaction/... ./internal/sddstatus/...` | `e2e/organicruntime` escalation case | revert `EscalationAccounting`, guard-pin test |
| 3 (Phase 3) | Stop-invariant table + SUSPECT routing | PR #1801 | `go test ./internal/cli/... -run ReviewStopInvariant` | `e2e/organicruntime` recovery scenarios | revert routing branches + invariant test |
| 4 (Phase 4) | Narration tier registry | PR #1801 | `go test ./internal/cli/... -run Narration` | `e2e/organicruntime/organic_lifecycle_hardening_test.go` paired scenario | revert `review_narration.go` + registrations |
| 5 (Phase 5) | Tool-fault defect report generation | PR #1801 | `go test ./internal/cli/... -run DefectReport` | `e2e/organicruntime` tool-fault terminal scenario | revert report builder + its two Tier-C wiring sites |
| 6 (Phase 6) | Self-derived recovery (A1-A4) | PR #1801 | `go test ./internal/cli/... -run ReviewRecover\|ReviewRepair\|FinalVerificationRetry\|ReopenResults` | `e2e/organicruntime` auto-recovery scenarios | revert facade seam; emitters stay, manual flow restored |
| 7 (Regression) | Cross-stream proof | PR #1801 | `go test ./...` | `go test ./e2e/organicruntime/...` | N/A — verification only |

## Phase 1: Assets — Auto Default (stream 5, first)

- [x] 1.1 RED: `assets_test.go` — add `TestSDDOrchestratorAssetsDefaultToAutomatic` asserting byte-identical "Automatic" default + prompt-budget sentence across all 12 runtimes; update `Pace: Interactive, Automatic.` anchor and `TestSDDFFCommandsHonorInteractiveMode`/opencode-preflight anchors for new wording. Fails on current "default to Interactive" text.
- [x] 1.2 GREEN: flip the default sentence + add one prompt-budget sentence, identical minimal diff, in `internal/assets/{antigravity,hermes,gemini,codex,qwen,kimi,kiro,opencode,generic,cursor,windsurf}/sdd-orchestrator*.md` and `internal/assets/claude/sdd-orchestrator-workflow.md`.
- [x] 1.3 GREEN: flip default only (keep `Interactive, Automatic` order) in the two preflight lists in `claude/sdd-orchestrator-workflow.md` and `opencode/sdd-orchestrator.md`. (Verified: the numbered/mapping preflight lists at claude:60/77 and opencode:137/154 already read `Interactive, Automatic`/`Interactive -> interactive; Automatic -> auto`; order was untouched — no edit needed there. The actual default flips live at claude/sdd-orchestrator-workflow.md:118 and opencode/sdd-orchestrator.md:210, both covered by 1.2.)
- [x] 1.4 Verify: `go test ./internal/assets/...` green.

## Phase 2: Visible Accounting (stream 3)

- [x] 2.1 RED: `internal/reviewtransaction` test asserting `CompactState.EscalationAccounting()` returns `Cause/Spent/Remaining/Total`, `Remaining` clamped at 0, labels matching `sddstatus/status.go:162-172` naming.
- [x] 2.2 GREEN: add `CompactEscalationAccounting` + `EscalationAccounting()` deriving from `CumulativeCorrectionLines`/`OriginalCriteria.Passed`/`CorrectionRegression.Passed` at the `compact.go:1171` seam — pure method, no persisted field, `Validate()` untouched.
- [x] 2.3 GREEN: wire spent/remaining/total, distinctly labeled, into the CLI surfaces emitting `StateEscalated`. (Site: `sddstatus.resolveBoundedRemediation`'s compact branch — added an explicit `compact.State == StateEscalated` early return using the new accounting, fixing a real gap where an already-escalated compact authority with criteria-failure-only escalation was incorrectly offered fresh `Required: true` remediation.)
- [x] 2.4 GATEKEEPER TASK: attempt RED for a negative `CorrectionBudgetRemaining` emission from `sddstatus/review_gate.go:190`. Verified: the `remainingBudget <= 0` guard (now at `:198` after 2.3's insertion, originally `:191`) returns early (`RemediationState{Reason: ...}`) before the field is ever populated (now `:210`, originally `:203`) — negative is unreachable via this call site. Resolved by writing a regression test PINNING the guard (spent >= total triggers the early return, `CorrectionBudgetRemaining`/`CorrectionBudgetTotal` never set). Recorded as refuted in the test's doc comment; no production fix.
- [x] 2.5 Verify: `go test ./internal/reviewtransaction/... ./internal/sddstatus/...`.

## Phase 3: No-Stop-With-Successor Invariant (stream 2)

- [x] 3.1 RED: `internal/cli/review_stop_invariant_test.go` — table-driven, reusing the `review_next_transition_docs_test.go:15` source-extraction harness (`reviewStopTransitionCallRegexp`), binding each stop code to a terminal-proof or caller-continuable entry (`reviewStopInvariantClassification`); fails closed on any unrouted/uncovered code (`TestReviewStopInvariantReasonCodesAreClassified`) and on any terminal-vs-docs contradiction (`TestReviewStopInvariantTerminalClassificationAgreesWithDocs`). RED proof for both fail-closed directions recorded by temporarily removing an entry and temporarily flipping a `Terminal` value — both produced the exact expected failure text (see apply-progress memory); restored byte-identical before GREEN. Note: after 3.4/3.8's fix, the code emits 14 distinct codes / 16 call sites, not 15/17 — `escalated_recovery_requires_changed_target` was removed by routing, not classified.
- [x] 3.2 Investigate `native_stop_required` (`:128`): confirmed genuinely terminal — only reachable for `StateEscalated` when target-changed, accounting-only, and final-verification-retry are all ineligible (`target_status.go:176-199`); matches docs. No code change.
- [x] 3.3 Investigate `unchanged_or_unverified_authority` (`:126`) — confirmed genuinely terminal; the accounting-only relief is specific to `StateEscalated`'s budget-only crossing and does not apply to `classifyCompactCorrectionTarget`'s `Blocked` claim (reached only via a genuine last-attempt failure or no scope-expansion/contraction/resume match), matching docs' "further work requires a new lineage". No code change.
- [x] 3.4 Investigate `escalated_recovery_requires_changed_target` (`:209`) — **found the unsoftened third case**: `RecoverCompactAuthority` already derives an evidence-bound recovery edge for an accounting-only escalation without requiring the target to change further (`deriveCompactRecoveredEvidence`), but `newReviewNextTransition`'s `StateEscalated` case re-checked target identity unconditionally and forced a false stop on that legal edge. Fixed by removing the redundant re-check (status.Action is trusted, as every other case in the switch already does) — see 3.8/3.10 for the routing and reclassification.
- [x] 3.5 Investigate `pre_pr_selector_unrepresentable` (`:180`) — confirmed no legal in-process route: the caller explicitly chose the pre-pr gate, and silently substituting a different gate would answer a different question. Reclassified caller-continuable (not terminal) per 3.10's general rule — docs row names a concrete `--base-ref` fix. No code change beyond classification.
- [x] 3.6 Investigate `manual_intervention_required` (`:217`) default branch — confirmed structurally unreachable in practice (every state the compact authority or a legacy chain can produce is handled by an earlier explicit case) — a correct defensive fallback, genuinely terminal if ever hit. No code change.
- [x] 3.7 Decide `reconcile-authority`: **Tier C, no A5 edge** — it operates on an already-created, already-invalid successor discovered out-of-band, not part of the status/next-transition state graph. Recorded in design.md's Open Questions for the Phase 4 registry.
- [x] 3.8 GREEN: landed the one proven-reachable SUSPECT fix as a routing branch (`internal/cli/review_next_transition.go`'s `StateEscalated` case, RED→GREEN via `TestNewReviewNextTransitionEscalatedRouting`'s updated subtests) — never a test exemption. `docs/review-integration.md`'s now-dead `escalated_recovery_requires_changed_target` row removed (`TestEveryReviewStopReasonCodeHasADocsContinuation` re-verified green).
- [x] 3.9 Verify: `go test ./internal/cli/... -run ReviewStopInvariant` green; full `go test ./internal/cli/...` green (98.8s).
- [x] 3.10 **Contradiction resolution (blocking, from the discoverability audit)**: `recovery_scope_unchanged`, `recovery_target_unrepresentable`, and `original_finalize_request_required` reclassified caller-continuable per the docs table, as required. Applying the same general rule (docs row not "Terminal"-prefixed ⇒ not terminal) structurally across all codes surfaced one additional pre-existing contradiction beyond the three named: `corrected_candidate_unavailable` was also misclassified terminal in design.md — its docs row names a concrete `review status --next-transition`/`review finalize` continuation. Reclassified alongside the named three; flagged explicitly in design.md and here as a finding beyond the audit's literal scope. design.md's Duty 3 table updated in this task.

## Phase 3b: In-band route enrichment (discoverability audit findings)

The blocking-state audit found ~70 blocking states, with the machine envelope naming the continuation in only ~55%. These tasks close the ranked gaps where recovery EXISTS but the block names nothing. Message/envelope-field changes only; no condition changes.

- [x] 3b.1 `receipt_missing` → `next_action: "review.start"`; `receipt_unrelated` → `next_action: "review.status"` (`review_operation_contract.go`, `newReviewIntegrationFailure`'s `discovery` switch) — the highest-likelihood real block: an agent running a gate before any review is told bare "stop" today. RED first on the envelope fields (`TestReceiptDiscoveryFailuresNameTheWayIn`), then GREEN; updated 4 pre-existing tests pinned to the old "stop" value (`review_receipt_discovery_test.go` ×3, all clean/committed baseline).
- [x] 3b.2 SDD ledger CLI errors name the status route: suffixed `ErrRuntimeBudgetExhausted`, `ErrRuntimeAttemptActive`, `ErrRuntimeNoActiveAttempt`, `ErrRuntimeObjectiveDone`, `ErrRuntimeNoObjective` (`runtime_ledger.go:46-51`, left `ErrRuntimeObjectiveChange` alone — not in the task's list) with the new `runtimeLedgerStatusPointer` const ("run `gentle-ai sdd-attempt status` — its next_action names the continuation"). RED→GREEN via new `internal/sddstatus/runtime_ledger_discoverability_test.go`. No existing test asserted exact `.Error()` text (only `errors.Is`), so safe.
- [x] 3b.3 Gate `escalated` denial: `action` → `"review.status"` instead of bare stop (`internal/cli/review.go`'s `reviewGateAction`, was `:574`); same for `final_verification_retry_denied` (`review_operation_contract.go`, was `:252`) — status re-derives retry eligibility and nothing told the agent so. RED→GREEN via `TestGateEscalatedDenialAndFinalVerificationRetryDenialNameStatus`; updated 3 pre-existing pinned tests (`review_test.go`, `review_final_verification_retry_test.go`, both clean/committed).
- [x] 3b.4 `legacy_read_only` negotiated envelope carries the same route its non-negotiated twin already names ("choose a new lineage for compact authority", `review_facade.go:1080`) (`review_operation_contract.go`, was `:479`) — `next_action` now `"review.start"`, message suffixed with the exact non-negotiated phrase. RED→GREEN via `TestLegacyReadOnlyNegotiatedEnvelopeCarriesTheNonNegotiatedRoute`; updated 3 pre-existing pinned tests (`review_failure_contract_test.go`, `review_operation_contract_test.go`, `review_status_contract_test.go`, all clean/committed) across all 4 legacy-collision shapes (start/finalize/review-step/invalidate) — the route is uniform because every shape means the same thing: this lineage ID is claimed by a read-only legacy chain, so a new lineage is the only way to compact/v2 authority regardless of which operation hit it.
- [x] 3b.5 `store_lock_unavailable` (`review_operation_contract.go`, was `:421`): named the wait-and-retry route, matching `authority_lock_timeout`'s existing shape exactly — `RetrySafe: true`, `next_action: "retry_with_bounded_backoff"` (advisory contention, not damage). RED→GREEN via `TestStoreLockUnavailableNamesTheWaitAndRetryRoute`; updated 1 pre-existing pinned test (`review_failure_contract_test.go`, clean/committed).
- [x] 3b.6 Negotiated decline and negotiated kill-switch-off previously fell through to `operation_outcome_unknown` with `mutation_outcome: unknown` — a false safety claim for operations that provably never started (both refuse inside `authorizeReviewStart`, strictly before any authority mutation). Added typed branches: `errReviewDeclinedForCandidate` → typed `review_declined` with `not_started`, `next_action: "review.start"` (never latched, so re-running simply asks again); `*reviewtransaction.RDDDisabledError` → typed `rdd_disabled` with `not_started`. RED first proving today's misclassification (`TestNegotiatedDeclineAndDisabledKillSwitchAreTypedNotStarted`), then GREEN. Treated as a correctness fix (asserted `MutationOutcome` directly), not a wording task.
- [x] 3b.7 Reviewer-result byte-conflict blocks name `review dispose-result` / `preserve-result` as the deciding operations (human decision stays human — the block just names it). **Correction to the task's own line citations**: `review_artifact.go:393`'s literal-bytes-conflict branch is structurally unreachable in practice — `readVerifiedReviewerArtifact` gates entry on the NEW payload's SHA-256 matching the stored file's hash, so reaching that branch with `!bytes.Equal` true requires a SHA-256 collision; the genuinely reachable manifestation of the same conflict is the `else if !os.IsNotExist(err)` branch immediately below it (surfacing `readVerifiedReviewerArtifact`'s `"artifact SHA-256 mismatch"`). Enriched both (defense-in-depth) plus `:570`'s digest conflict, all three now naming `review dispose-result`/`review preserve-result`. RED→GREEN via `TestReviewerResultAndDigestByteConflictsNameTheDecidingOperations`, which discovered this reachability gap live (first RED failure was "artifact SHA-256 mismatch" wanting "review dispose-result", not the expected literal-bytes message).
- [x] 3b.8 **Investigated deadlock candidate — CONFIRMED, resolved-by-report (organic-dx Phase 5 maintainer decision)**: `receipt_publication_conflict` (`review_operation_contract.go`, `ImmutablePublicationConflictError` via `inspectCompactFacadeReceipt`/`newFacadeReceiptPublicationError` in `review_facade.go`) fires when an already-terminal-committed authority's derived receipt bytes differ from an existing immutable `receipt.json` at the same CAS path. Exhaustively checked every command in the `review` dispatch table (`reclaim` targets incomplete store entries only; `reconcile-authority`/`reconcile-authority-batch` target invalid recovery-successor edges only; `review repair`'s `AuthorityRepairConflicting` status is a *different* conflict class — multiple legacy-v1-alias candidates across lineages, not a receipt-vs-authority byte mismatch) — none resolves this shape. **No resolving command exists.** Per the maintainer decision recorded for this apply run, this is classified a TOOL-FAULT terminal: it has no resolving command by confirmed investigation, and a generated defect report (plus one Tier-C statement naming the report path and the issues URL) is its designed exit for this release; designing a resolving command is deferred post-release. `newFacadeReceiptPublicationError` (`review_facade.go`) now threads `ctx`/`root` and, only on a genuine `ImmutablePublicationConflictError`, calls `reviewGenerateToolFaultDefectReport` and appends the clause to `ReviewFacadeReceiptPublicationError.Error()`; the existing `next_action: "explicit-maintainer-action"` in the negotiated envelope is untouched.
- [x] 3b.9 **Investigated deadlock candidate — CONFIRMED, resolved-by-report (organic-dx Phase 5 maintainer decision)**: captured final EVIDENCE byte conflict (`review_artifact.go:69`, `RunReviewCaptureEvidence`, wrapped via `reviewPreflightError` so its specific text is genericized to `invalid_request` in the negotiated envelope) — dispose-result/preserve-result exist for reviewer *results* only, not final evidence; there is no `dispose-evidence`/analogous command. The only same-lineage path that technically exists is non-designed and unsafe to recommend (finalize with the unwanted evidence and hope verification fails). **No resolving command exists.** Per the maintainer decision, classified a TOOL-FAULT terminal alongside 3b.8, same designed exit. `RunReviewCaptureEvidence`'s byte-conflict branch (`review_artifact.go`) now calls `reviewGenerateToolFaultDefectReport` and appends the clause to the wrapped error; the existing `reviewPreflightError` genericization to `invalid_request` in the negotiated envelope is untouched. Designing a `dispose-evidence`-shaped command (or documenting the abandon-and-restart path) remains deferred post-release, per the maintainer decision.
- [x] 3b.10 `corrected_candidate_unavailable` (`review_next_transition.go:155`) and `staged_workspace_overlay_recovery_unavailable` (`:93`, both line numbers confirmed unchanged after Phase 3/3b edits): the stop stays schema-bare by design (no implementation here — Phase 4 is out of scope for this run). Recorded as Phase-4 registry inputs: `corrected_candidate_unavailable`'s Tier-C statement content = "Change the candidate content so it differs from the frozen original, then re-run `gentle-ai review status --next-transition` (or `review finalize`)."; `staged_workspace_overlay_recovery_unavailable`'s Tier-C statement content = "Pass `--lineage <id>` to recover an existing lineage, or drop `--workspace-overlay` and run `gentle-ai review start --projection staged` to start fresh." — both copied verbatim from their docs/review-integration.md continuation rows per spec Tier C's "the single decision or command" requirement.
- [ ] 3b.11 **Community finding (ardelperal, PR comment 2026-07-26, cross-referenced in #1837)**: the `next_transition.collect` envelope emits BOTH `--repository-context` and a default `--cwd` in `inputs[].arguments`, but `review capture-result` rejects the pair ("accepts either --repository-context or --cwd, not both") — a copy-paste-verbatim failure in the executable-as-printed family (1745). Fix the argument assembly to emit exactly one canonical form (the negotiated caller context knows which it holds); RED first proving the emitted pair is rejected by the real command. Scheduled with Phase 6 (same file surface).

## Phase 4: Narration Tier Registry (stream 4, after Phase 3)

- [x] 4.1 RED: `TestReviewNarrationRegistryCoversEveryStopReasonCode` in `internal/cli/review_narration_test.go` — reuses the same `reviewStopTransitionCallRegexp` source-extraction Phase 3's stop-invariant test already proves exhaustive. RED proof recorded both directions (temporarily removing a registry entry, and temporarily leaving a stale one) — both produced the exact expected failure text; restored byte-identical before GREEN.
- [x] 4.2 RED: `TestNarrationTierAAndCBanInternalVocabulary` over the registry's rendered strings — bans `lineage/ordinal/CAS/facade/receipt/revision/digest` (whole-word, outside backtick code spans) and `sha256:`, plus uncertainty phrases (`I'll figure`, `I don't know`, `look into`, `not sure`, `try to`). RED proof recorded by temporarily injecting the word "lineage" into a Tier C statement's prose (outside backticks) — produced the exact expected failure; reverted before GREEN.
- [x] 4.3 GREEN: implemented `reviewNarrationTier` registry (`internal/cli/review_narration.go`) keyed by stable emission ID (`stop:<code>`, `consent:<name>`, `escalation:accounting`); registers 3 sources — the 14 `review_next_transition.go` stop codes -> Tier C (one statement each, matching `docs/review-integration.md`'s continuation table in domain terms; `corrected_candidate_unavailable`/`staged_workspace_overlay_recovery_unavailable` carry the exact content task 3b.10 recorded), the consent-prompt constants (`review_mode.go`, referenced directly — not copied — so the registry cannot drift from what actually prints) -> Tier A, and the Phase-2 escalation accounting statement -> Tier A (extracted the format string into `sddstatus.EscalationAccountingReasonTemplate` as the single source of truth for both the production call site and the registry sample). Growth rule documented in the registry's doc comment and enforced structurally by 4.1's test for the stop-code source. **Decision**: the internal-vocabulary ban strips backtick-quoted spans before checking (`reviewNarrationStripCodeSpans`) — a literal CLI flag like `` `--lineage <id>` `` is unavoidable public contract syntax a caller must type, not narrative jargon exposing internal architecture; the ban targets prose leaking those concepts, not the flag names themselves. Flagged as an interpretive decision for maintainer review (design.md did not resolve this tension explicitly).
- [x] 4.4 RED (E2E, paired requirement): `TestOrganicReviewNarrationPairedRecoverableVersusTerminal` in `e2e/organicruntime/organic_lifecycle_hardening_test.go`. Chosen "one underlying machinery failure": `TargetApplicabilityUnrelated` (no existing review record matches this target) in `newReviewNextTransition`, with two caller-selector shapes — (a) the ordinary shape reaches an executable `fresh_target_ready` transition with zero output on stderr (Tier-A/uninterrupted, Tier-B silent); (b) the same missing-record condition requested through `--workspace-overlay --projection staged` (recovery-only for a fresh target) is terminal here and emits exactly one Tier C statement on stderr, byte-identical to the registered `staged_workspace_overlay_recovery_unavailable` text, while stdout's JSON envelope stays unchanged either way. Wired the actual production narration call site to make this observable: `runReviewStatus` (`review_facade.go`) now calls `reviewNarrateStopReason` on stderr only when `next-transition` resolves to `Kind: "stop"` — this call site did not exist before this task; RED proof recorded by temporarily removing the wiring (test failed with empty stderr on the terminal subtest), then restoring it.
- [x] 4.5 Verify: `go test ./internal/cli/... -run Narration` and `go test ./e2e/organicruntime -run TestOrganicReviewNarrationPairedRecoverableVersusTerminal` both green (see apply-progress memory for verbatim output; no test literally named `OrganicLifecycleHardening` exists as a single top-level test, so the new paired test's own name is used instead).

## Phase 5: Tool-Fault Defect Reports (stream 4 extension, maintainer addition)

Terminal Tier-C states split into two causes: a legitimate USER decision (abandon, permanent disable) and a genuine TOOL-INTERNAL fault (an invariant-violation-class stop). Only the latter generates a report. Storage: `<GitCommonDir>/gentle-ai/defect-reports/` — reuses the existing `GitCommonDir`-relative CAS storage convention (`repository_locator.go`), so it never lands in the working tree and is never committed.

- [x] 5.1 Classify: added a `ToolFault *bool` column (`internal/cli/review_stop_invariant_test.go`'s `reviewStopDisposition`, meaningful only when `Terminal: true`) — every one of the 9 terminal-proof rows from 3.1 classified: tool-fault (literal "internal invariant violation" / structurally-unreachable justification) = `captured_result_selection_unavailable`, `final_verification_retry_unavailable`, `manual_intervention_required`, `missing_authority_binding`; user-decision (legitimate data-integrity finding or by-design business-rule terminal, not a code defect) = `captured_artifacts_unverifiable`, `corrupted_or_unverifiable_authority`, `native_stop_required`, `staged_workspace_overlay_recovery_unavailable`, `unchanged_or_unverified_authority`. New `TestReviewStopInvariantToolFaultColumnIsWellFormed` enforces every terminal entry has a classification and every caller-continuable entry stays nil. **Note**: this column is documentation only — the two reason codes Phase 5 actually wires a runtime report trigger for (`receipt_publication_conflict`, captured-final-EVIDENCE conflict) are not `review_next_transition.go` stop codes at all; see 3b.8/3b.9.
- [x] 5.2 RED: `TestReviewDefectReportRendersTemplateShapedFields` (`internal/cli/review_defect_report_test.go`) — asserts `newReviewDefectReport(...).render()` produces Markdown with section headers matching `.github/ISSUE_TEMPLATE/bug_report.yml`'s field labels (`Bug Description`, `Steps to Reproduce`, `Expected Behavior`, `Actual Behavior`, `Gentle AI Version`, `Operating System`, `AI Agent / Client`, `Affected Area`, `Logs / Error Output`), populated from version+commit (`runtime/debug.ReadBuildInfo`, mirroring `internal/app.ResolveVersion` without importing it), OS/arch, the operation shape, the verbatim reason/error code, opaque state identifiers, and the terminal precondition. Confirmed RED via the initial compile failure (`undefined: newReviewDefectReport` etc.) before the production file existed.
- [x] 5.3 RED (privacy, hard requirement): `TestReviewDefectReportScrubsPoisonedInput` and `TestReviewDefectReportScrubbedFieldIsSingleLineAndBounded` (`internal/cli/review_defect_report_test.go`) drive the builder with `$HOME`-rooted absolute paths, diff hunks, multi-line file contents, env-var-shaped values (`KEY=secret`), and email addresses across every field including `StateIdentifiers` — assert none reach `render()`. RED proof recorded twice: (1) the initial compile failure: (2) after implementing, temporarily reverting `StateIdentifiers` to the general (non-strict) scrub and re-running — reproduced a live leak (`content: the quick brown fox file contents should never appear` in the rendered report), proving the stricter identifier-value scrub (`reviewScrubDefectReportIdentifierValue`) is load-bearing; restored before GREEN.
- [x] 5.4 GREEN: implemented `reviewDefectReport`/`newReviewDefectReport`/`.render()` (`internal/cli/review_defect_report.go`) satisfying 5.2 and 5.3. Scrub pipeline per field: truncate at first `\n` (kills diffs/file contents/multi-line payloads), redact `KEY=VALUE`-shaped env tokens, redact email-shaped substrings, redact absolute-path tokens (POSIX and Windows-drive forms) — all via `reviewScrubDefectReportField`; `StateIdentifiers` values additionally go through `reviewScrubDefectReportIdentifierValue`, which redacts the entire value if a space survives the general scrub (opaque identifiers never contain a space; a survaving space is presumptively narrative content). `writeReviewDefectReport` writes to `<GitCommonDir>/gentle-ai/defect-reports/<reason-code-slug>-<sha256-content-hash prefix>.md` via `reviewtransaction.OpenRepositoryIdentityLease(...).Identity().GitCommonDir` — the same Git-common-dir CAS convention `repository_locator.go`/`store.go` already use, so it is outside the working tree and never committed.
- [x] 5.5 GREEN: wired generation into exactly the two confirmed tool-fault sites (3b.8/3b.9, not the 3.1 table's four unreachable-in-practice codes, which have no live call site to wire) via `reviewGenerateToolFaultDefectReport`: `review_facade.go`'s `newFacadeReceiptPublicationError` (now threading `ctx`/`root`, generating only on a genuine `*reviewtransaction.ImmutablePublicationConflictError`) and `review_artifact.go`'s `RunReviewCaptureEvidence` byte-conflict branch. User-decision Tier-C sites (`abandon`, `dispose-result` — the reviewer-result byte conflict from 3b.7 — and permanent disable) generate no report; proved directly by the E2E negative case in 5.7 and by construction (report generation is not called from those sites).
- [x] 5.6 GREEN: `reviewToolFaultDefectClause(reportPath)` (`internal/cli/review_defect_report.go`) appends exactly one clause — report path + `https://github.com/Gentleman-Programming/gentle-ai/issues/new/choose` — to the base fault message at both wired sites (still one statement, one action; report-write failure degrades to an empty clause rather than masking the original fault). Registered a representative rendering in the Phase-4 narration registry as `defect_report:tool_fault` (Tier C), covered by `TestNarrationTierAAndCBanInternalVocabulary`.
- [x] 5.7 E2E: `TestOrganicReviewDefectReportToolFaultVersusUserDecision` (`e2e/organicruntime/organic_lifecycle_hardening_test.go`). Positive: drives the captured-final-EVIDENCE byte conflict (capture-evidence twice with different bytes against the same validating target/revision) — asserts exactly one report file exists under `<GitCommonDir>/gentle-ai/defect-reports/`, its Markdown matches every template-shaped header, stderr names both the exact report path and the issues URL, and the report contains none of the isolated `$HOME`, the worktree path, or either raw evidence payload's content. Negative: drives the reviewer-result byte conflict (3b.7, capture-result twice with different bytes for the same slot) — asserts stderr names `review dispose-result`/`review preserve-result` (a human decision) and that the defect-reports directory gains no file. RED proof recorded by temporarily reverting the `review_artifact.go` wiring and re-running: the positive subtest failed (`defect-reports directory = [], want exactly one report`) while the negative subtest still passed unchanged; restored before GREEN.
- [x] 5.8 Verify: `go test ./internal/cli/... -run DefectReport` and `go test ./e2e/organicruntime -count=1 -timeout=15m` both green (see apply-progress memory for verbatim output).

## Phase 6: Self-Driving Recovery (stream 1, last — authority-touching)

**Scope delivered this session: A1 (`review recover`) only, done to full TDD standard with all five fail-closed negatives genuinely proven.** A2 (`repair`)/A3 (`retry-final-verification`)/A4 (`reopen-results`) auto-consumption, the remaining 8 `--prepare`/`--preflight` emitters (6.6), the `review_repair.go` help-text move (6.11, N/A without A2 wired), the abandon/dispose-result Tier-C registry entries (6.12), and the three legacy-command emission-only flags (6.13) were investigated for feasibility but NOT implemented in this run — see the per-task notes below for why, and the apply-progress memory for the full investigation record. Per the run's own honesty contract ("a truthful three-of-four beats a forced four"), this is a truthful one-of-four done without shortcuts, not four rushed.

- [x] 6.1 RED: `reviewAuditActor` fallback chain (`user.name/email` -> email alone -> `GIT_AUTHOR_EMAIL` -> `GIT_COMMITTER_EMAIL` -> `gentle-ai-self-recovery@localhost`), `t.TempDir()` git repos; absolute/nested/relative `--cwd` (via `t.Chdir`) yield identical identity. Landed as `TestReviewAuditActorFallbackChain` (`internal/cli/review_audit_actor_test.go`). RED confirmed: undefined symbols before the production file existed.
- [x] 6.2 RED: `reviewAuditActor` rejects `\r`/`\n` in identity values and falls through. Landed as `TestReviewAuditActorRejectsNewlineInjectedIdentity` — poisoned `user.email` (via a raw `.git/config` double-quoted `\n` escape, proven non-vacuous via a positive control reading the raw value back) falls all the way to the literal fallback; poisoned `user.name` alone falls through to a safe email; a poisoned `GIT_AUTHOR_EMAIL` falls through to `GIT_COMMITTER_EMAIL`.
- [x] 6.3 GREEN: implemented `reviewAuditActor(ctx, root)` (`internal/cli/review_audit_actor.go`) — `exec.CommandContext(ctx, "git", "-C", root, "config", "--get", key)`, read-only, every error/unsafe-value swallowed and falls through, terminal fallback is the fixed literal `gentle-ai-self-recovery@localhost`.
- [x] 6.4 RED: closed per-shape reason enum test — `TestReviewSelfRecoveryReasonIsAClosedNamedEnum` (every defined constant renders a non-empty, uniquely-worded, `self-recovery: `-prefixed reason) and `TestReviewSelfRecoveryReasonRejectsAnUnknownShape` (an unregistered shape value renders empty rather than echoing free text) — `internal/cli/review_self_recovery_reason_test.go`.
- [x] 6.5 GREEN: implemented `reviewSelfRecoveryReason(shape)` and the closed `reviewSelfRecoveryShape` enum (`internal/cli/review_self_recovery_reason.go`) — 5 constants, one per A1 trigger state named in design's Duty 1 table row 1 (`recover_invalidated`, `recover_correction_required`, `recover_approved`, `recover_escalated_changed_target`, `recover_escalated_accounting_only`). **Deliberately scoped to A1 only**: defining unused Repair/RetryFinalVerification/ReopenResults constants without any wired consumer or test would be untested dead code: left for the follow-up run that wires A2-A4.
- [ ] 6.6 GREEN: generalize the `--prepare`/`--preflight` emitter to the remaining gated commands lacking it (emission only, all 10). **NOT DONE.** Investigated: `repair` and `reopen-results` already have an emitter; the other 8 (`recover`, `retry-final-verification`, `reconcile-authority`, `abandon`, `dispose-result`, and the three `*-legacy-*` commands) do not. Adding 8 new read-only preflight modes, each needing its own RED/GREEN pair and help-text update, was assessed as its own multi-hour unit of work beyond this session's remaining budget; doing it hastily risked exactly the kind of unverified, fabricated-passing claims the honesty contract forbids.
- [x] 6.7 RED for shape A1 (`recover`) only: auto-derivation applies when `--maintainer-authorization` is absent (`flags.Visit` presence-check via the pre-existing `reviewFlagWasProvided`, not an empty-value check). Landed as `TestReviewRecoverSelfDerivesBindingForInvalidatedPredecessor` and `TestReviewRecoverSelfDerivedBindingFallsBackToEnvActor` (`internal/cli/review_recover_self_derivation_test.go`). RED confirmed: both failed with the old unconditional `--reason`/`--actor`-required error before the production wiring landed. **A2/A3/A4 not attempted** — see 6.6's note; each needs its own binding-derivation function (e.g. repair's authorization binding builder lives unexported in `internal/reviewtransaction/authority_repair.go` and would need a new exported wrapper) and its own eligibility-shape mapping, none of which existed as a reusable seam the way `reviewTransitionRecoveryAuthorization` already did for recover.
- [x] 6.8 GREEN for A1 only: wired the `!reviewFlagWasProvided(flags, "maintainer-authorization")` branch inside `RunReviewRecover` (`internal/cli/review_facade.go`) — the exact seam design's Duty 2 names (its cited `:806-807` line numbers had drifted forward across Phases 3-5's edits to the file; the seam is now the seam is the pre-existing successor-bound/predecessor-bound `reviewTransitionRecoveryAuthorization` normalization block, confirmed by direct reading, now preceded by the new derivation branch). New `reviewSelfRecoveryShapeForRecover(predecessorState, targetChanged)` (`internal/cli/review_self_recovery_reason.go`) picks the reason shape from data already in scope; it never gates legality — `reviewtransaction.RecoverCompactAuthority` still performs every existing check on the derived values exactly as on operator-supplied ones. The required-fields check was restructured: `--predecessor-lineage`/`--expected-predecessor-revision`/`--successor-lineage`/`--disposition` stay unconditionally required; `--reason`/`--actor` are required only when `--maintainer-authorization` is explicitly supplied (preserving the manual flow byte-for-byte). **A4 (`reopen-results`) routing into next-transition NOT done** — out of scope alongside A2/A3 per 6.7's note.
- [x] 6.9 RED x4 (negative) for A1 only, all landed in `review_recover_self_derivation_test.go`: `TestReviewRecoverSelfDerivationExplicitWrongBindingStillRefuses` (uses the escalated/target-changed shape, the one A1 sub-shape that actually validates the authorization value downstream — the invalidated shape does not check authorization at all, before or after self-derivation, so it was the wrong fixture for this negative and was corrected mid-task); `TestReviewRecoverSelfDerivationCorruptedAuthorityStillRefuses` (corrupted predecessor JSON, `Load()` fails before the derivation branch is ever reached); `TestReviewRecoverSelfDerivationActiveAttemptNeverAutoResets` (a `StateReviewing` predecessor is outside `reviewSelfRecoveryShapeForRecover`'s closed set, so derivation refuses to fabricate a binding — defense in depth alongside `RecoverCompactAuthority`'s own unconditional refusal of every disposition for that state); `TestReviewRecoverSelfDerivationFailedCriteriaEscalationNeverAutoRecovers` (a genuinely `StateEscalated`, unchanged-target predecessor whose escalation came from an over-budget *forecast*, not a completed correction attempt with passed criteria — self-derivation confidently emits the accounting-only reason and a matching binding, but `RecoverCompactAuthority`/`validateCompactRecoveryEdge` still refuse with `errCompactRecoveryTargetUnchanged`, proving self-derivation adds no new authority).
- [x] 6.10 RED for A1: `TestReviewRecoverSelfDerivationNoDriftElectiveResetStillRefuses` — a `scope_changed` recovery over an approved workspace-overlay predecessor whose live target genuinely has not drifted still refuses with the pre-existing, untouched "recovery scope has not changed" error; that check runs entirely before the derivation branch.
- [ ] 6.11 GREEN: `review_repair.go:109` help-text move. **NOT DONE** — N/A without A2 (`repair`) auto-derivation wired; moving this assertion now would be dishonest (repair's `--preflight` output genuinely still never emits an authorization template, so the sentence stays true as-is).
- [ ] 6.12 GREEN: register `abandon`/`dispose-result` as Tier-C in the Phase-4/5 narration registries. **NOT DONE** — deferred alongside 6.6 for the same reason (own unit of RED/GREEN work with its own registry-completeness test interaction); left for the follow-up run.
- [ ] 6.13 GREEN: emission-only binding flags for the three `*-legacy-*` commands. **NOT DONE** — deferred alongside 6.6.
- [x] 6.14 Verify: `go test ./internal/cli/... -run 'ReviewRecover|ReviewRepair|FinalVerificationRetry|ReopenResults'` green (48 subtests, 0 failures, ~5.1s) — every pre-existing recovery/repair/retry-final-verification/reopen-results test still passes byte-identically alongside the new self-derivation tests.

## Phase 7: Regression (explicit, cross-stream, last)

**Scope note**: since only A1 was wired in Phase 6, items below are reported against A1's actual delivered surface, not the full A1-A4 scope the phase originally anticipated.

- [x] 7.1 Happy path (A1 only, CLI-level integration test rather than a new named `e2e/organicruntime` scenario — see honesty note below): `TestReviewRecoverSelfDerivesBindingForInvalidatedPredecessor` drives `review recover` with zero authorization-shaped flags (no `--actor`, `--reason`, or `--maintainer-authorization`) and asserts the persisted CAS recovery record carries a real derived actor/reason — i.e. the operation completed with zero authorization tokens typed. Writing this as a NEW top-level `e2e/organicruntime` scenario (full agent-harness happy path showing "exactly one RDD consent prompt") was not attempted in the remaining budget; the property it would prove is already proven end-to-end through the real `RunReviewRecover` CLI entry point exercised by this test, which is the same production code path an e2e scenario would drive.
- [x] 7.2 The five fail-closed negatives as real tests (not new named `e2e/organicruntime` scenarios — same honesty note as 7.1): all five pass, see 6.9/6.10 above.
- [x] 7.3 A1 explicit-binding path stays byte-identical: every pre-existing `review recover` test that supplies `--maintainer-authorization` explicitly (`TestReviewRecoverEscalatedBaseDiffSuccessorOverCurrentChangesPredecessor`, `TestReviewRecoverEscalatedRequiresExactSuccessorBoundAuthorization`, `TestReviewRecoverInvalidatedRequiresInvalidatedPredecessor`, `TestReviewRecoverRetainsWorkspaceOverlayBaseAndScope`, `TestReviewRecoverAdoptsExplicitWorkspaceOverlayBase`, `TestReviewRecoverSelectsAuthorizedStagedProjection`, `TestReviewRecoverProjectionFailuresDoNotMutateAuthority`, `TestReviewRecoverReleaseScopeExpandsMergedSliceToFirstParentDiff`, and others) passed unmodified — none needed a single edit. A2/A3/A4 have no self-derivation wiring to regress in the first place.
- [x] 7.4 Interactive mode still selectable: unaffected by this session's changes (Phase 6 touches only authorization derivation, not execution-mode resolution); `go test ./internal/cli ./internal/reviewtransaction/... ./internal/sddstatus -count=1` (which includes the Phase 1 asset/anchor tests) passed green, confirming no incidental regression.
- [x] 7.5 Defect report privacy under the full E2E suite: `go test ./e2e/organicruntime -count=1 -timeout=15m` passed green (79.2s), including `TestOrganicReviewDefectReportToolFaultVersusUserDecision` from Phase 5 — unaffected by this session's changes.
- [x] 7.6 Verify: `go test ./...` and `go test ./e2e/organicruntime/...` both green — see apply-progress memory for verbatim output. **Found and fixed one genuine pre-existing gap** (not caused by this session, discovered only because 7.6 is the first `go test ./...` this whole apply run has ever done — every earlier phase's verify command was scoped to specific packages): 11 `internal/components` golden fixtures (`testdata/golden/*.golden`) still pinned the pre-Phase-1 "default to **Interactive**" sentence, unrelated to `internal/assets`' own already-corrected text. Regenerated via `go test ./internal/components/ -run TestGolden -update`; `git diff --stat` confirmed exactly one line changed per file, all the identical stale-to-corrected wording swap, nothing else — the exact "stale behavior being corrected" exemption for editing an existing test's expectation.

## Phase 3c: Kill-switch coherence for plural stale receipts (community blocker, run AFTER Phases 4-5)

**Community finding (decode2, PR #1801, 2026-07-26T08:23)** — investigated and root-caused. Premise correction: gentle-ai has NEVER installed a git hook (verified across full history); decode2's pre-commit hook is user-owned wiring, but prescribed by v2.1.11's own shipped guidance ("Before commit, stage all reviewed paths, then validate pre-commit"), so the upgrader cohort with such a hook is real. The gentle-ai-owned defect is the gate's exit code, not the hook.

Root cause: `discoverCompactFacadeGateReview` (`internal/cli/review_facade.go:~2301`) emits `receipt_ambiguous` whenever 2+ receipts are individually stale. Each of those receipts ALONE classifies `receipt_scope_changed` → unmanaged-while-disabled → exit 0. Two together → `receipt_ambiguous`, which is NOT in `reviewReceiptDiscoveryIsUnmanagedWhileDisabled` (`:~2929`) → exit 1, blocking an ordinary commit with reviews OFF. Multiple terminal receipts are the NORM for any active RDD user (no pruning; overlapping genesis paths classify scope-changed), so this hits the upgrade path broadly. No in-product remediation exists while disabled: `--lineage` routes to direct evaluation where the disabled classification never applies.

- [x] 3c.1 RED: the cross-product fixture nobody built — reviews DISABLED × 2+ terminal lineages with overlapping genesis paths → assert today's exit 1 / `receipt_ambiguous`, and assert each receipt alone yields exit 0 / `disabled/unmanaged`. Landed as `TestReviewValidatePluralStaleReceiptsReportDisabledUnmanagedDelivery` (`review_disabled_delivery_test.go`): builds lineage A (workspace review of `docs/plural-stale.md`), drifts the file, disables, and validates — proving the single-stale-receipt case alone already reports `disabled/unmanaged` (denial code `candidate-or-paths-mismatch`) — then adds lineage B via `cloneApprovedDiscoveryAuthority` (bypasses the CLI's own disabled-mode gate on `review start`, exactly as a review earned before disabling would already sit on disk) and re-validates. RED genuinely reproduced by temporarily forcing `staleOnly := false` at the composition site and the classifier's `ReviewReceiptAmbiguous` case to `return false` (see apply-progress memory for the exact revert/restore): the two-lineage case failed with `receipt_ambiguous`/exit 1 as today's code does; restored before GREEN.
- [x] 3c.2 GREEN: added `DeterministicallyStaleOnly bool` to `ReviewReceiptDiscoveryError` (`internal/cli/review_facade.go`) — a plain bool chosen over a distinct `ReviewReceiptDiscoveryKind` because Kind is wire-facing (JSON `code`, docs continuation table, Phase 4's narration registry); splitting it would ripple through all three for a distinction with no separate wire behavior of its own. Set at the ambiguous-return site inside `discoverCompactFacadeGateReview` (`staleOnly := len(scopeWithoutContext) == 0 && len(assessmentUnknown) == 0`, i.e. composed solely of `scopeChanged` + `deliveryShape` + `targetResolution`). `reviewReceiptDiscoveryIsUnmanagedWhileDisabled` (kept its name, changed its signature from `Kind` to `*ReviewReceiptDiscoveryError`) now returns `discovery.DeterministicallyStaleOnly` for the `ReviewReceiptAmbiguous` case instead of unconditional `false`; both call sites updated. `reviewReceiptDiscoveryIsUnmanagedWhileDisabled` itself was NOT widened to accept `receipt_ambiguous` unconditionally, per the non-negotiable constraint.
- [x] 3c.3 NEGATIVE (non-negotiable), all three proven: (1) `len(exact) > 1` still fails closed while disabled — `TestReviewValidateKeepsFailingClosedOnMultipleExactReceiptsWhileDisabled` (new fixture: two lineages independently approved over identical exact candidate bytes, then disabled) — this composition never sets `DeterministicallyStaleOnly`, so it is structurally unaffected by 3c.2; the single-exact-receipt-stays-authoritative counterpart was **already covered** by the pre-existing `TestReviewValidateKeepsGoverningReceiptAuthoritativeWhileDisabled`, cited rather than duplicated. (2) `assessmentUnknown`/`scopeWithoutContext` undecidable mixtures still fail closed — proven directly at the classifier boundary via `TestReviewReceiptDiscoveryIsUnmanagedWhileDisabledRejectsUndecidableAmbiguousCompositions` (a synthetic `DeterministicallyStaleOnly: false` `ReviewReceiptAmbiguous` value, plus a positive control proving the assertion discriminates rather than being vacuously true); fabricating a live `assessmentUnknown`/`scopeWithoutContext` fixture through the full discovery pipeline would require forcing an untyped `AssessCompactGateTarget` error or a `CompactScopeChangeDiagnostics` failure — both already unit-tested at their own layer in `internal/reviewtransaction` — so this was deliberately proven at the classifier's decision boundary instead of reproducing that machinery end-to-end. (3) `authority_corrupted` untouched — confirmed by code inspection (its `Kind` never routes through the `ReviewReceiptAmbiguous` case) and by the pre-existing, unmodified `TestReviewValidateKeepsFailingClosedOnCorruptedAuthorityWhileDisabled*` tests staying green.
- [x] 3c.4 The ambiguous denial names its remediation. Implemented via `ReviewReceiptDiscoveryError.Error()` (used by the bare/non-negotiated `review validate` path — the actual shape of a pre-commit hook, i.e. decode2's report) and the negotiated failure contract's `ReviewReceiptAmbiguous` switch case (`review_operation_contract.go`) — both now fold `discovery.Candidates` into the message text for the `DeterministicallyStaleOnly` composition. The `len(exact) > 1`/undecidable composition's message is untouched (byte-identical, per 3c.5).
- [x] 3c.5 Enabled-mode: **scope extended mid-task by the maintainer** (see below) — `len(exact) > 1` and undecidable-mixture compositions stay byte-identical (proven: the pre-existing, unmodified `TestUnqualifiedGateDiscoveryRejectsMultipleExactReceiptsButExplicitLineageIsDirect` still passes verbatim; the undecidable case shares the identical `else` branch in `review_operation_contract.go` as `exact > 1`, so that same pin covers it). The deterministically-stale-only composition's enabled-mode denial changed by design (see below) and its regression pin is the updated `TestUnqualifiedGateDiscoveryRequiresSelectionForMultipleScopeChangedReceipts`.
- [x] 3c.6 Same-pass candidate: typed. The two duplicate `errors.New(...)` call sites in `runReviewFacadeValidate`'s mixed compact-v2/legacy-v1 check now share `errReviewMixedCompactLegacyAuthority` (`internal/cli/review_facade.go`), matchable via `errors.Is`, byte-identical message. Deliberately NOT routed through the `ReviewReceiptDiscoveryError`/disabled-reclassification machinery and NOT reclassified as unmanaged-while-disabled — full justification recorded in the sentinel's doc comment: `discoverCompactFacadeGateReview` returning `compactErr == nil` here means the compact receipt already proved it EXACTLY governs, so a legacy chain also exactly matching is present-tense competing authority (the same shape as `len(exact) > 1`, just across two store systems), not proven non-governance — reclassifying would mean silently picking one authority system over the other. Confirmed zero pre-existing test coverage of this branch (in either mode) by search before this task; added `TestUnqualifiedGateDiscoveryFailsClosedOnMixedCompactAndLegacyAuthority` (`review_receipt_discovery_test.go`, reusing `newLegacyCLIFixture` + `finalizeFacadeReviewForRepo` over the identical unchanged candidate) proving both enabled and disabled modes still fail closed — a genuine behavior pin, not a RED→GREEN test, since typing alone changes no behavior. A third, unrelated occurrence of the same message exists in `RunReviewInvalidate` (`:953`) — explicit-`--lineage`-only, mutating, not reachable from an unattended gate hook — confirmed out of scope and left untouched.

**Scope extension (maintainer, mid-task course correction, adopted)**: the same defect exists with reviews ENABLED, in the REASON rather than the outcome. Before this task, plural individually-stale receipts with reviews ON produced `receipt_ambiguous`/`AuthorityApplicability: "ambiguous"`/`NextAction: "review.status"` — sending the caller to disambiguate history, which is false: nothing governs this candidate at all. Blocking stays correct (the candidate genuinely was never reviewed), but the reason and continuation were both wrong. Fixed at both `runReviewFacadeValidate`'s bare-error path (shared with 3c.4 via `Error()`) and the negotiated failure contract: for the `DeterministicallyStaleOnly` composition only, `AuthorityApplicability` becomes `"not_evaluated"`, `NextAction` becomes `"review.start"`, `RequiredInputs` becomes `[]`, and `Message` states nothing governs the candidate while naming prior lineages as optional (never required) recovery. `Code` stays `"receipt_ambiguous"` (Kind unchanged, minimizing wire-format churn) and the exit code/blocking stays unchanged — only the stated reason and continuation changed. **Deliberately NOT touched**: `review status`'s own independent ambiguity classification (`internal/reviewtransaction/target_status.go`, `TargetApplicabilityAmbiguous`/`TargetStatusActionSelectLineage`) — a structurally separate subsystem with much larger blast radius (Phase 3/3b stop-invariant territory) that may have an analogous framing gap; flagged as a follow-up candidate, not fixed here. The existing `TestUnqualifiedGateDiscoveryRequiresSelectionForMultipleScopeChangedReceipts` test's `review status` assertions (after the validate denial) were left as-is since that subsystem is unchanged — an agent following `NextAction: "review.start"` never needs to call `review status` first, so no contradiction results from leaving it unchanged.

**Deferred post-release (documented, decode2's corroborating signal)**: SDD-side mode coherence — `ErrRuntimeRemediationSuccessorRequired` (`internal/sddstatus/runtime_ledger.go:60`, triggered `:455-457`) demands a terminally-approved successor lineage that can never exist while reviews are disabled; nothing in `internal/sddstatus` consults the kill switch. Rarer (needs an in-flight remediation binding at disable time) and has workarounds (re-enable to close, or reset the objective). Conditioning an append-only ledger invariant on a mutable global switch needs calm design, not a rushed patch. Minimum viable meanwhile: a typed error naming the disabled-mode deadlock instead of the bare successor demand.

## Phase 3d: Bound discovery cost as lineages accumulate (non-destructive)

**Maintainer decision (2026-07-26)**: receipt garbage collection is POST-RELEASE (destructive: a bug loses someone's proof, and the community tests on disposable repos so it cannot be validated by this loop). What ships now is the non-destructive half — stop paying a growing per-gate cost for lineages that can never govern again. Retention itself is untouched: nothing is deleted, moved, or compacted here.

Why it matters: every gate call runs `CompactAuthorityLeaves` (`internal/reviewtransaction/compact_store.go:554`), which loads EVERY store and then assesses leaves against the live candidate. Lineages accumulate forever with no pruning, so the cost grows without bound — a latent timeout cliff under the facade deadline, not a correctness bug.

**Hard constraint discovered while scoping**: `compactAuthorityLeaves` (`:580`) validates the whole recovery graph — every successor's predecessor must be present and its edge re-validated, else "dangling predecessor". So a lineage that is a chain predecessor MUST still be loaded. Scoping can only skip the EXPENSIVE per-leaf work, never the graph load.

- [x] 3d.1 MEASURE FIRST (no code change): build a fixture repo with N terminal lineages (N = 1, 10, 50) and profile where gate-time actually goes — `store.Load()` (JSON + chain validation) vs the per-leaf `AssessCompactGateTarget` git subprocesses. Report the breakdown. If the cost is NOT dominated by per-leaf assessment, STOP and report — the premise would be wrong and the fix would be elsewhere. **Premise confirmed, not stopped.** Throwaway harness (`internal/reviewtransaction`, N unrelated genesis-disjoint terminal leaves, `gitCommandContext` seam counted subprocess invocations, deleted after measuring — no code change survives this task): N=1 load+graph=2.4-3.0ms vs per-leaf-assess=9.2-10.3ms (7 git subprocesses); N=10 load+graph=3.6-4.4ms vs per-leaf-assess=94.6-98.1ms (70 subprocesses); N=50 load+graph=7.0-9.0ms vs per-leaf-assess=473-493ms (350 subprocesses, ~9.5ms/leaf constant). Load+graph stays ~2-9ms across N (its own growth is real but two orders of magnitude smaller); per-leaf assessment scales linearly with zero batching and dominates by 55-65x at N=50. Reran twice for stability, numbers consistent within noise.
- [x] 3d.2 RED: a test asserting the number of per-leaf assessments performed for a candidate when several terminal lineages provably cannot govern it (instrument the assessment call, or assert on git-subprocess count if a seam exists — find the existing seam, e.g. `gitCommandContext`, before inventing one). New hookable seam `assessCompactGateTargetForDiscovery` (`internal/cli/review_facade.go`, mirrors the pre-existing `writeCompactFacadeReceipt` var-hook pattern in the same file) wraps the single production call site of `reviewtransaction.AssessCompactGateTarget` inside `discoverCompactFacadeGateReview`. `TestPreCommitGateDiscoverySkipsAssessmentForGenesisDisjointTerminalLeaves` (`internal/cli/review_receipt_discovery_test.go`) builds 12 real, terminal, approved, genesis-disjoint lineages via the production `approveDiscoveryMarkdown` flow, counts assessments via the seam, and asserts fewer than 12. RED genuinely reproduced: temporarily gated the skip with `if false && ...`, reran, observed `assessed == 12` (old bare-loop behavior), then restored.
- [x] 3d.3 GREEN: skip the expensive per-leaf assessment for leaves that provably cannot govern the live candidate, using only data already loaded in the record (e.g. genesis-path disjointness from the live changed paths). Loading and graph validation stay untouched. New `internal/reviewtransaction/compact_gate_discovery_baseline.go`: `BuildCompactPreCommitDiscoveryBaseline` resolves the live pre-commit candidate ONCE (not per leaf); `CompactLeafProvablyUnrelatedToPreCommitBaseline` is a zero-git, in-memory predicate over already-loaded `CompactState` fields. **Scope decision (documented in the type's doc comment): GatePreCommit only**, not all five gates. Rationale, empirically proven: PreCommit's staged projection makes `buildCompactLifecycleSnapshot` force `IntendedUntracked` to empty and `validateCompactUntrackedScope` short-circuit to nil (compact_gate.go:531-533, :684-706) and `lifecycleTargetForGate` always resolves PostApply/PreCommit to `TargetCurrentChanges` regardless of the leaf's own `Kind` except FixDiff/BaseWorkspaceOverlay (gate.go:1250-1257) — so ONE resolved live snapshot is BYTE-IDENTICAL (not approximate) to what any ordinary (non-"exact-recommit") leaf would individually resolve. GatePostApply's own untracked-scope check (a repo-wide check, not skippable without replicating it) and the other three gates' genuinely per-lineage-varying target resolution (push/PR boundary, release range) were examined and found NOT to share this same byte-identical-baseline property without materially higher risk to the byte-identical non-negotiable — left for a future task rather than attempted unsafely. `discoverCompactFacadeGateReview` computes the baseline lazily (only if `input.Gate == GatePreCommit` and at least one terminal candidate exists) and skips the seam call (a plain `continue`, matching the bookkeeping-only handling every other `CompactGateTargetUnrelated` result already gets) when the predicate proves non-governance.
- [x] 3d.4 NON-NEGOTIABLE — outcomes byte-identical: for every lineage that CAN govern or that classifies into any discovery bucket used by the gate (`exact`, `scopeChanged`, `deliveryShape`, `targetResolution`, `assessmentUnknown`, `scopeWithoutContext`), the emitted result must be byte-identical to today. The skip may only remove work whose outcome is provably "does not govern, contributes nothing". Regression test over the existing discovery fixtures asserting identical JSON before/after. Proof is two-layered: (a) new `TestPreCommitGateDiscoveryStillSelectsExactReceiptAmongDisjointNoiseLineages` — 8 genesis-disjoint noise lineages plus one genuinely governing exact receipt under GatePreCommit; the exact receipt is still selected (only 2 of 9 leaves needed the real assessment: the governing one plus the one whose frozen candidate tree coincidentally equals the shifted HEAD, correctly excluded from the fast path by the `CandidateTree == baseline.HeadTree` guard). (b) the FULL pre-existing discovery suite (`TestUnqualified*`, `TestUnscoped*`, `TestReceiptless*`, `TestExplicitMalformed*`, `TestRelease*` in `internal/cli`) ran unmodified and stayed green, including `TestUnqualifiedGateDiscoveryRequiresSelectionForMultipleScopeChangedReceipts/staged` (GatePreCommit, two overlapping-not-disjoint scope-changed candidates — never fast-pathed, `DeterministicallyStaleOnly` and `Candidates` unchanged) — no golden-JSON-diff harness exists in this codebase's test style for this function, so this is the equivalent proof available: identical assertions, unmodified, still passing.
- [x] 3d.5 Interaction with Phase 3c: a skipped leaf must not silently change the ambiguity composition. If skipping alters which lineages appear in `Candidates`, that is a behavior change — either keep them listed or STOP and report the conflict. No conflict: the fast path only ever fires when genesis paths are PROVABLY disjoint from the live candidate (i.e., the leaf would have resolved to `CompactGateTargetUnrelated`, which already contributes to NEITHER `Candidates` NOR `DeterministicallyStaleOnly` today); `allLineages` (used for the `receipt_unrelated` all-candidates listing) is built before the fast-path check runs, so that listing is unaffected either way. Phase 3c's own scope-changed composition test (cited in 3d.4) confirms no change to that classification.
- [x] 3d.6 Verify: focused tests, `go test ./internal/cli ./internal/reviewtransaction/... -count=1`, `go test ./e2e/organicruntime -count=1 -timeout=15m`. All green — see verification log below.

**Deferred post-release (documented)**: reachability-based collection + blob compaction. Rule established this session: a lineage is collectable when no live successor references it (successors store `PredecessorLineageID`/`PredecessorRevision` and are validated against them) AND nothing shipped under its authority. Delivered lineages compact to a receipt stub (~4KB proof retained, evidence blobs up to 4MB dropped); superseded-and-unreferenced lineages are fully collectable; quarantined incidents are kept deliberately. Full deletion stays behind an explicit command.

## Phase 3e: `review status`'s own stale-only ambiguity classification (the Phase 3c follow-up)

**Origin**: Phase 3c's scope-extension notes explicitly flagged this as a follow-up: `internal/reviewtransaction/target_status.go`'s `assessTargetStatusSnapshot` has its own, independent classification with the same shape Phase 3c fixed in `discoverCompactFacadeGateReview` — zero EXACTLY governing candidates plus 2+ STALE (scope-changed) lineages reported `TargetApplicabilityAmbiguous`/`TargetStatusActionSelectLineage` (a required disambiguation), while zero candidates and fewer than 2 stale lineages correctly reported `TargetApplicabilityUnrelated`/`TargetStatusActionStart`. One stale receipt let the caller start; two stale receipts sent them to disambiguate history — the same "stale receipts must inform, never decide" gap.

- [x] 3e.1 Fix `assessTargetStatusSnapshot` (`internal/reviewtransaction/target_status.go`, `~255-263`): when `len(candidates) == 0 && len(scopeChangedCandidates) > 1`, report the identical shape the `case 0` branch already reports (`TargetApplicabilityUnrelated`/`TargetStatusActionStart`/`ReplayabilityNotReplayable`), including that branch's own `TargetBaseWorkspaceOverlay`+`ProjectionStaged` safety-stop override (`TargetStatusActionStop`/`ReplayabilityManualActionRequired`) — determined this DOES apply here too, since it is a live-projection safety check (driven by `live.Kind`/`live.Projection`, the same variable both paths read) independent of lineage history, not a history-driven decision. `CandidateLineageIDs` stays populated with the stale lineages so recovering one remains a discoverable OPTION, never a required chore. The pre-existing `default:` branch (2+ EXACTLY governing/recoverable candidates — real present-tense competing authority) is untouched.
- [x] 3e.2 NEGATIVE (non-negotiable) #1 — 2+ EXACTLY governing candidates stay `ambiguous`/`select_lineage`, byte-identical: covered by the pre-existing, unmodified `TestAssessTargetStatusClassifiesAllApplicabilityStates/ambiguous` subtest (untouched code path, `default:` branch); additionally pinned by new `TestAssessTargetStatusKeepsExactAmbiguityWhenStaleLineagesAlsoExist`, which proves the guard doesn't leak even when stale lineages ALSO coexist alongside the 2 exact ones.
- [x] 3e.3 NEGATIVE (non-negotiable) #2 — zero governing + exactly ONE stale lineage stays byte-identical to today (`Unrelated`/`Start`, empty `CandidateLineageIDs`): new `TestAssessTargetStatusKeepsSingleStaleLineageUnrelatedWithoutCandidateListing`.
- [x] 3e.4 Positive proof (non-negotiable) #3 — stale lineage IDs remain listed on the new path: `TestAssessTargetStatusReportsPluralStaleLineagesAsUnrelatedWithOptionalRecovery` (plain `current-changes` target, 2 stale lineages via `writeApprovedTargetStatusHistory` + commit + drift) and `TestAssessTargetStatusReportsPluralStaleStagedOverlayLineagesAsUnrelatedWithStop` (proves the overlay+staged Stop override composes correctly with the fix — required a fixture whose review origin is itself `TargetBaseDiff` queried from a base that already advanced past the reviewed diff, so `compactApprovedStagedScopeRecoveryShape`'s `next.BaseTree == initial.BaseTree` requirement is broken and both lineages fall through to the generic scope-changed classifier instead of the per-candidate recovery-eligible one). All RED-verified by temporarily reverting the production fix and re-running before restoring it.
- [x] 3e.5 Course correction during this task: the pre-existing `TestAssessTargetStatusReportsAmbiguousApprovedStagedScopeExpansion` was initially (incorrectly) suspected to be an instance of this same bug and edited to expect the new `Unrelated`/`Stop` shape. Running it against the fix immediately falsified that: both its lineages are independently eligible for `compactApprovedStagedScopeRecovery` (a real, per-candidate recovery match requiring a `TargetBaseDiff` origin reviewed against the SAME base the live query uses), so both land in `candidates`, not `scopeChangedCandidates` — a genuinely different, legitimate "2+ recoverable candidates compete for the same recovery slot" bucket that must stay `ambiguous`/`select_lineage`. Reverted to its original, unmodified assertions (now also serving as 3e.2's staged-specific companion) and its doc comment updated to record the investigation and the empirical falsification, per the honesty contract.
- [x] 3e.6 Downstream consumer #1 — `internal/cli/review_status_contract.go`'s `ReviewTargetStatusResult.Validate()` (a real production consumer: called from `internal/cli/review_facade.go:747` before every negotiated `review status --contract` JSON emission) previously REQUIRED `len(result.Candidates) == 0` for `TargetApplicabilityUnrelated` — this would have rejected the new shape outright (verified: RED reproduced a real `RunReview(["status", "--contract", ...])` failure with `operation_failed` before the fix). Relaxed to allow non-empty `Candidates` for `Unrelated` (optional recovery, not required); all other Unrelated invariants (`Authority`/`Frozen`/`AuthorityTargetIdentity`/`Receipt`/`Action` shape) untouched. New `TestNegotiatedReviewStatusValidateAcceptsOptionalCandidatesForUnrelatedTarget` proves both the empty and non-empty `Candidates` shapes validate. JSON schema (`status-v2.schema.json`) needed no change — it never constrained `candidates` by `applicability`.
- [x] 3e.7 Downstream consumer #2 — `internal/cli/review_next_transition.go`'s `newReviewNextTransition` (grepped both files per the assignment). Its `TargetApplicabilityUnrelated` case never reads `status.Candidates` — routing to `review.start` (or `stop` for the overlay+staged case) is unaffected by their presence. Already proven by the PRE-EXISTING `TestReviewNextTransitionStateTable`, whose `status()` helper sets `Candidates: []string{"first", "second"}` on every table row including all four `TargetApplicabilityUnrelated` cases, and which passes unchanged both before and after this fix (the code path is untouched) — cited rather than duplicated, per the assignment's "if a consumer genuinely depends on the old classification... STOP" clause: it does not depend on it, so no change was needed or made.
- [x] 3e.8 Existing tests updated (stale framing, both cited with file:line and why): (a) `internal/reviewtransaction/target_status_test.go` — see 3e.5 above (the staged-scope test was investigated, found to be a different legitimate bucket, and reverted — net zero behavioral change, doc comment updated only). (b) `internal/cli/review_status_contract_test.go:894-928` `TestNegotiatedReviewStatusCompletesWithOneHundredHistoricalLeaves` — 100 distinct lineages, all scope-changed relative to a drifted live target, previously asserted `Ambiguous`/`SelectLineage`; this IS the exact stale-history-decides framing this phase corrects, updated to `Unrelated`/`Start`/`ReplayabilityNotReplayable` plus an explicit `status.Validate()` call, `len(Candidates) == 100` unchanged (still fully discoverable). (c) `internal/cli/review_receipt_discovery_test.go:463-480` inside `TestUnqualifiedGateDiscoveryRequiresSelectionForMultipleScopeChangedReceipts` (Phase 3c's own test, its `discoverCompactFacadeGateReview`/`validate` assertions earlier in the same test are UNTOUCHED — a structurally separate subsystem, exactly as Phase 3c's notes said) — its trailing `review status --contract` assertion was the literal follow-up Phase 3c named; updated to `Unrelated`/`Start`/`ReplayabilityNotReplayable` with `Candidates` still listing both lineages, plus an explicit `status.Validate()` call. Discovered empirically: this test regressed when the production fix landed, confirming it was genuinely exercising the same stale framing rather than being unrelated.
- [x] 3e.9 Verify: `go run ./internal/gofmtcheck` (clean), `go vet ./...` (clean), `go test ./internal/cli ./internal/reviewtransaction/... ./internal/sddstatus -count=1` (all `ok`), `go test ./e2e/organicruntime -count=1 -timeout=15m` (`ok`, ~82s).

## Phase 3f: Community findings from the Refresh 4 exploratory battery (fisidj)

Reported by tester fisidj (Windows + OpenCode, Refresh 4, exploratory fixtures beyond guide flows 1-13), PR #1801 comment 2026-07-26T10:58. Finding 1 is INDEPENDENT CONFIRMATION of decode2's kill-switch blocker — three receipts, clean repo, no inherited hook — already fixed in Phase 3c; fisidj's repro is the cleaner one and must become a test fixture. The rest are new.

- [x] 3f.1 Adopted fisidj's repro as an explicit fixture: `TestReviewValidateReportsDisabledUnmanagedDeliveryOverThreeStaleReceiptsAtPrePush` (`internal/cli/review_disabled_delivery_test.go`) — enable reviews, real bare remote, THREE separate reviewed+finalized docs commits (first two actually pushed), disable, one unreviewed docs commit, `review validate --gate pre-push` → asserts `disabled/unmanaged`, exit 0. No production fix needed (Phase 3c's `DeterministicallyStaleOnly` classification already generalizes); genuinely RED-proved by temporarily forcing `DeterministicallyStaleOnly: staleOnly && false` at the `review_facade.go` composition site — reproduced the exact old `receipt_ambiguous`/exit-1 failure with three receipts — then restored, confirming the fix is not count-specific.
- [x] 3f.2 **Finalize reviewer-result refusals now name the continuation.** Sites (line numbers had drifted from the task's citation due to earlier phases' edits): `internal/cli/review_facade.go` `prepareCompactReviewerResults` (~2654, "review finalize requires all %d original reviewer result(s)"), `internal/cli/review_artifact.go` `readFacadeReviewerArtifacts` (~643, "...reviewer artifact(s)") and `readCapturedReviewerResults` (~726, "...captured reviewer result(s)"). All three now append "capture each missing one with `gentle-ai review capture-result` (see `<next-transition refresh command>` for the exact lineage/target/lens/order bindings)". The command name is derived from a new shared `reviewCaptureResultCaptureOperation` constant (`internal/cli/review_next_transition.go`) also used by `reviewCaptureInput` (the collect transition's own canonical builder), so it cannot drift; the binding-lookup pointer reuses the pre-existing `reviewNextTransitionRefreshCommand` constant rather than hand-building lineage/revision/lens/order tokens at each site (avoids embedding a static, potentially-stale binding in an error string). RED confirmed as a compile failure (`undefined: reviewCaptureResultCommandName`) before implementation; `internal/cli/review_finalize_reviewer_result_discoverability_test.go` covers all three sites.
- [x] 3f.3 **Spaced boolean flags — DECISION RECORDED: kept the parser strict (option b), refusal names the `=` form.** Rejected accepting the detached form CLI-wide (parser change, wide blast radius across every command). Implemented at the single site every review verb's flag parsing already funnels through — `parseReviewFlags` (`internal/cli/review.go`) — via new `reviewBooleanFlagSpacedValueHint`: after a successful `flags.Parse`, if the first leftover positional is exactly `true`/`false` and the immediately preceding raw argument names a registered boolean flag without `=`, the refusal becomes "boolean flag --X takes --X or --X=true, not a separate value; got \"true\"" instead of the bare `unexpected review <verb> argument "true"`. Every verb benefits with zero per-verb changes (proved on both `review start --committed-only` and `review status --action-eligibility` in the same test). RED confirmed as a real behavioral failure (old bare message) before the fix; genuinely unrelated extra arguments keep their original refusal unchanged. `internal/cli/review_boolean_flag_spacing_test.go`.
- [x] 3f.4 **Install completion copy now names only the agents actually installed.** `internal/verify/report.go`'s `readyMessage` became the neutral fallback "You're ready. Start building."; new exported `ReadyMessageForCommands(commands []string)` renders "You're ready. Run `X` [or `Y`] and start building." for exactly the given commands. `internal/cli/run.go`'s `withPostInstallNotes` gained `withReadyAgentRunNote`, scoped to the two agents the original line named (claude, opencode) — deliberately not expanded to all 12 CLI-runnable agents, an unreviewed scope decision left for a future task. The override fires only when `FinalNote` is still the exact generic production text, so it never clobbers a hand-constructed test fixture or another note-builder's text. `sync.go`'s post-sync report path was not given agent-specific naming (out of this task's "first-run experience" scope) but automatically stopped over-claiming once `readyMessage`'s base text became agent-neutral. RED confirmed as a compile failure before `ReadyMessageForCommands`/`verify.ReadyMessage` existed. `internal/verify/ready_message_test.go`, `internal/cli/run_ready_agent_note_test.go`.
- [x] 3f.5 **`doctor` now also names the invoked executable's own path and version.** `internal/cli/doctor.go`'s `checkOneTool`, for the `gentle-ai` tool only, appends "; invoked executable: <path> (version <version>)" via new `doctorInvokedGentleAIClause`, and an explicit "-- this differs from the PATH-resolved copy above..." clause when `doctorSameExecutable` (symlink-tolerant path comparison) finds the invoked binary and the PATH-resolved one are not the same file. New overridable `osExecutableDoctor = os.Executable` var follows the file's existing test-seam convention; version resolution reuses the pre-existing `reviewGentleAIVersionAndCommit()` (Phase 5's local mirror of `internal/app.ResolveVersion`, avoiding an import cycle) rather than duplicating it. RED confirmed as a compile failure before `osExecutableDoctor` existed. One pre-existing test (`TestRunDoctor_IntegrationAllMocked`, `internal/cli/doctor_test.go:447-509`) needed updating: it did not mock the new seam, so it exercised the real `os.Executable()` (the test binary) against a mocked PATH resolution and correctly failed by reproducing the exact ambiguity this task closes; updated to mock `osExecutableDoctor` to match the PATH-resolved copy (the common case) and updated the expected output to include the new clause — cited here per the non-negotiable evidence requirement. `internal/cli/doctor_invoked_binary_test.go` covers the new behavior directly.
