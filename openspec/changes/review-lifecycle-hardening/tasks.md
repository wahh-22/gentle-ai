# Tasks: Review Lifecycle Hardening

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~900-1200 (7 groups, 19 named E2E subtests, unit tables, 2 new files) |
| 400-line budget risk | High |
| Chained PRs recommended | No — `size:exception` accepted |
| Suggested split | Single PR #1801, 7 independently revertible commits inside it |
| Delivery strategy | exception-ok |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: High

Exception status: **exception-ok** — maintainer accepted `size:exception` for PR #1801. Size never stops or splits this chain; do not recommend chained PRs.

## Constraints

- Do NOT touch uncommitted #1825/#1827 changes: `internal/cli/run.go`, `internal/cli/review_mode.go`, `internal/components/agentguidance/inject.go`, and their tests.
- Short fixes only. 1778 is a constant + selector, not a timeout framework. 1745 emits `--flag=value` argv tokens, not a parser change.
- STRICT TDD: every task below is RED-first — write the failing test, run it, observe failure, then implement GREEN.
- Delivery order is fixed: **E → A → B → G → F → C → D**. C and D land last (they move fail-closed boundaries).

### Suggested Work Units

| Unit | Goal | PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| E | START-scoped deadline | PR #1801 | `go test ./internal/cli -run TestReviewFacadeOperationDeadlineSelector` | `go test ./e2e/organicruntime -run TestOrganicReviewStartDeadlineHeadroom -v` | Revert `review_facade.go` deadline selector hunk only |
| A | Error typing (delivery-base, 1699, 1666/1807) | PR #1801 | `go test ./internal/reviewtransaction -run TestDiscoverCompactFacadeGateReviewClassification|TestArtifactAdmissionCandidateCausal` && `go test ./internal/cli -run TestNewReviewIntegrationFailureCause` | `go test ./e2e/organicruntime -run TestOrganicReviewLifecycleErrorTyping -v` | Revert `gate.go`, `artifact_admission.go`, `review_operation_contract.go` hunks |
| B | Executable contract (1745,1775,1663,1788,1800) | PR #1801 | `go test ./internal/cli -run TestReviewTransitionArgumentToken|TestReviewSchemaVerificationEvidenceEntry|TestReviewFacadeFinalizeStateValidating|TestNewReviewNextTransitionEscalatedRouting` | `go test ./e2e/organicruntime -run TestOrganicReviewExecutableTransitionContract -v` | Revert `review_next_transition.go`, `review_schema.go`, `review_facade.go` finalize hunks |
| G | Target shapes (1812,1771,1641) | PR #1801 | `go test ./internal/cli -run TestReviewFacadeStagedProjectionBaseRefRefusal` && `go test ./internal/reviewtransaction -run TestResolveCurrentChangesBaseUnbornHead|TestFirstPublicationEmptyBaseReceiptRefusal` | `go test ./e2e/organicruntime -run TestOrganicReviewTargetShapeRefusals -v` | Revert `review_facade.go:938-940` and `snapshot.go` refusal hunks |
| F | Platform fail-safes (1781,1804) | PR #1801 | `go test ./internal/reviewtransaction -run TestSecureOpenLockParent|TestPublishImmutableDarwinENOTSUP` | `go test ./e2e/organicruntime -run TestOrganicReviewPlatformFailSafes -v` (both subtests `t.Skip` on Linux, reason recorded) | Revert `secure_open_unix.go`, `publish_immutable_darwin.go` hunks |
| C | Recovery graph (1744,1816,1782) | PR #1801 | `go test ./internal/reviewtransaction -run TestCompactRecoveryTargetKindAdmissible|TestCompactReleaseScopeRecoveryParentCount|TestSelectCompactPrePRChainRootIncoming` then full `go test ./internal/reviewtransaction/...` | `go test ./e2e/organicruntime -run TestOrganicReviewRecoveryGraph -v` | Revert `compact_store.go` kind-admissibility + parent-relax hunks, `compact_chain.go` root-exemption hunk, `review_facade.go:669-671` |
| D | Store robustness (1813) | PR #1801 | `go test ./internal/reviewtransaction -run TestCompactSemanticStateErrorTyped|TestCompactLineageQuarantinable|TestCompactAuthorityLeavesQuarantine|TestAuthorityInventoryDiagnosticQuarantine` | `go test ./e2e/organicruntime -run TestOrganicReviewStoreRobustness -v` | Revert `compact.go`, `compact_store.go` quarantine hunks, `target_status_projection.go`, `status.go` |

## Phase 1 — Group E: START-specific deadline (1778)

- [x] 1.1 RED: `TestReviewFacadeOperationDeadlineSelector` table test (start → large const, every other op → 25s) in `internal/cli/review_facade_test.go`. Observe failure (selector does not exist).
- [x] 1.2 GREEN: add `reviewFacadeStartOperationTimeout` const + `reviewFacadeOperationDeadline(operation string) time.Duration` at `review_facade.go:255`; general `reviewFacadeOperationTimeout` var stays byte-identical; wire `review.start` call site to the selector.
- [x] 1.3 RED→GREEN E2E: `TestOrganicReviewStartDeadlineHeadroom/issue-1778` in new `e2e/organicruntime/organic_lifecycle_hardening_test.go` — large valid workspace exceeding 25s completes under the start deadline.
- [x] 1.4 Regression: `review_status_contract_test.go:848-861` and `review_final_verification_retry_test.go:261-264` (direct-var mutation) pass unmodified.

## Phase 2 — Group A: Error typing (published-delivery pre-push, 1699, 1666, 1807)

- [x] 2.1 RED: `TestDiscoverCompactFacadeGateReviewClassification` ladder table on `gate.go:748` (merge-base ambiguity), `:752` (unconfigured remote), `:1153` (delivery-base ambiguity) in `internal/reviewtransaction`.
- [x] 2.2 GREEN: typed `*GateDeliveryBaseResolutionError` sentinel; route `:1153` through the existing `deliveryShape` bucket (`review_facade.go`) into `ReviewReceiptScopeChanged`. **Deviation from this task's original wording** (see Deviations section below): `:748` and `:752` route as `*GateTargetResolutionError` (target-resolution failure), not into `deliveryShape`/`authority_corrupted`, per the amended `specs/review-findings-ledger/spec.md` scenario "Untyped push-target ambiguity types as target-resolution failure (1699-adjacent target typing)" which is authoritative over this task's stale wording.
- [x] 2.3 RED: `TestArtifactAdmissionCandidateCausalCanonicalization` — differently-ordered/formatted candidate-causal IDs admit; a canonicalization error returns `ArtifactAdmissionIncomplete`.
- [x] 2.4 GREEN: split predicate at `artifact_admission.go:214-217`; canonicalize both sides before comparing; `ArtifactAdmissionOutOfScope` message stays byte-identical for real set mismatches.
- [x] 2.5 RED: `TestNewReviewIntegrationFailureCause` — additive `Cause` field populated from `runErr.Error()` on the `operation_outcome_unknown` default envelope. (The `reviewIntegrationPreflightError` → phase preflight/not_started/RetrySafe-true branch already existed in `review_operation_contract.go:449-454`; no change was needed there.)
- [x] 2.6 GREEN: `review_operation_contract.go` add `Cause string` (+ JSON schema `cause` property) populated only on the true mutating-operation catch-all; the read-only content-free catch-all stays byte-identical and never carries `Cause`. `Code`/`Message`/`MutationOutcome` stay byte-identical elsewhere.
- [x] 2.7 RED→GREEN E2E in `TestOrganicReviewLifecycleErrorTyping`: `issue-1801-published-delivery` (disabled → `disabled/unmanaged`; enabled → typed scope-changed, not corruption), `issue-1699`, `issue-1666`, `issue-1807`.
- [x] 2.8 Regression: `reviewReceiptDiscoveryIsUnmanagedWhileDisabled` unit table unchanged (Missing/Unrelated/ScopeChanged=true; Ambiguous/Corrupted=false); pre-push-before-push allow; `--committed-only` unaffected; governing-receipt allow (`review_disabled_delivery_test.go:115` sibling pattern). Full `go test ./internal/cli ./internal/reviewtransaction` passes.

## Phase 3 — Group B: Executable contract (1745, 1775, 1663, 1788, 1800)

- [x] 3.1 RED: `TestReviewTransitionArgumentToken` — every `Execute.Arguments` entry carries exact `--flag=value` argv token; `Preconditions` never carry `Token`.
- [x] 3.2 GREEN: add `Token string` to `ReviewTransitionArgument`; single-point tokenization in `reviewExecuteTransition` (mechanically hyphenates `Name`, fixing `captured_results` → `--captured-results=true`); `Name`/`Value` stay byte-identical.
- [x] 3.3 RED→GREEN E2E: `issue-1745` — printed token runs literally without reformatting.
- [x] 3.4 RED: `TestReviewSchemaVerificationEvidenceEntry` — schema catalog includes `gentle-ai.review-verification-evidence/v1` (key `verification-evidence`), shape derived from `readCapturedFinalEvidence` (raw non-empty content, bounded by `reviewResultArtifactLimit`).
- [x] 3.5 GREEN: add entry + usage string, `review_schema.go`.
- [x] 3.6 RED→GREEN E2E: `issue-1775`.
- [x] 3.7 RED: `TestReviewFacadeFinalizeStateValidating` two-subtest table — (a) 1663: canonical captured evidence exists, empty request evidence, not low-risk → consumed, not no-op; (b) 1788: already-consumed → typed `ErrReviewFinalizeNoTransition` naming verbatim `gentle-ai review capture-evidence` then `gentle-ai review finalize --lineage <id> --captured-evidence`.
- [x] 3.8 GREEN: caller-side auto-consume of canonical captured evidence before `prepareFacadeFinalizePlan`; typed rejection replaces the silent `len(plan.Transitions)==0` success **only** when `!*nextTransition` (a `--next-transition` request stays a deliberate read-only routing projection, matching the existing zero-transition comment/regression `TestValidatingEvidenceCollectionUnblocksFinalizeAndPreCommit`).
- [x] 3.9 RED→GREEN E2E: `issue-1663`, `issue-1788`.
- [x] 3.10 RED: `TestNewReviewNextTransitionEscalatedRouting` — `StateEscalated` routes to `reviewRecoveryCollection` with `RecoveryEscalated` forced; Stop with a new reason code (`escalated_recovery_requires_changed_target`) when `TargetIdentity == reviewAuthorityTargetIdentity(status)`, regardless of Selector presence.
- [x] 3.11 GREEN: `review_next_transition.go` `StateEscalated` case routing change.
- [x] 3.12 RED→GREEN E2E: `issue-1800` — escalated authority with a changed target surfaces the executable escalate-to-recover continuation with `disposition=escalated`.
- [x] 3.13 Regression: `Name`/`Value` bytes unchanged for existing non-Execute consumers (full `internal/cli` suite passes; one pre-existing `reflect.DeepEqual` validator (`validateStartNextTransition`) updated to tokenize its expectation the same way).

## Phase 4 — Group G: Target shapes (1812, 1771, 1641)

- [x] 4.1 RED: `TestReviewFacadeStagedProjectionBaseRefRefusal` — `--projection staged` + `--base-ref` without `--workspace-overlay` refused; message names both real escapes verbatim (see Deviations).
- [x] 4.2 GREEN: insert typed refusal at `review_facade.go` (before the dirty-tracked check).
- [x] 4.3 RED→GREEN E2E: `issue-1812`.
- [x] 4.4 **Pin anchor first**: confirmed at `internal/reviewtransaction/snapshot.go:813-839` (`resolveCurrentChangesBase`) — the selector-free (non-staged) branch returns the raw `git rev-parse HEAD` error unconditionally at the `headErr == nil || projection != ProjectionStaged` guard, instead of falling through to the unborn-detection walk the staged branch already runs. **Community evidence (reporter lu149e) confirms the same site from a second angle**: plain `gentle-ai review start --cwd $PWD` (default workspace projection, direct path, no `--projection` flag) on a genuinely unborn-HEAD repo reproduced verbatim `build facade review target: git rev-parse --verify HEAD^{tree} failed with exit code 128: fatal: Needed a single revision` — proven by reverting only `snapshot.go` and re-running `TestReviewFacadeUnbornHeadDefaultProjectionStart`, which reproduced that exact string byte-for-byte. This is the SAME defect as the selector-free `review status` gap (both reach `TargetCurrentChanges` + `ProjectionWorkspace` through `resolveCurrentChangesBase`), not a second one; no new issue needed.
- [x] 4.5 RED (only after 4.4 confirms the anchor): `TestResolveCurrentChangesBaseUnbornHeadSelectorFree` — selector-free status on unborn HEAD resolves to empty-tree projection, not a raw Git error.
- [x] 4.6 GREEN: route through the same empty-tree projection the staged path already uses (widened the guard to `headErr == nil` only, so every projection falls through to the existing unborn-detection walk).
- [x] 4.7 RED→GREEN E2E: `issue-1771`.
- [x] 4.8 RED: `TestFirstPublicationEmptyBaseReceiptRefusal` — first publication from an empty-base receipt refused; message names verbatim "commit an authorized empty root, then run committed base-diff review" (see Deviations for the real anchor).
- [x] 4.9 GREEN: `compact_gate.go:195-197` message update (real anchor; see Deviations).
- [x] 4.10 RED→GREEN E2E: `issue-1641`.

## Phase 5 — Group F: Platform fail-safes (1781, 1804)

- [x] 5.1 RED: `TestSecureOpenLockParentAnchor` — under-root walk succeeds; not-under-root falls back to today's root-anchored walk verbatim; symlinked component refused. Root resolved via `secureLockRoot` (the fixed `gentle-ai/review-transactions` marker), injected directly as the function's new `root` parameter for the unit test.
- [x] 5.2 GREEN: `secureOpenLockParent(root, path)` anchored at the resolved Git common-dir (`secure_open_unix.go`); `openRARPathNoFollow` (unrelated security boundary) keeps its exact prior behavior by always passing the filesystem root as anchor.
- [x] 5.3 RED: `TestNegotiatedStoreLockPreAcquisitionFailureIsNotStarted` — pre-lock failure reports `not_started`, not unknown-mutation.
- [x] 5.4 GREEN: new `*StoreLockPreAcquisitionError` wraps every `acquireLocalStoreLock` failure up to (not including) genuine lock acquisition; classified `not_started`/`pre_native` in `review_operation_contract.go`, mirroring the existing Git-failure family.
- [x] 5.5 RED: `TestPublishNoReplaceDarwinENOTSUPFallsBackToCopy` (+ never-replaces, other-errno-propagates siblings) — `ENOTSUP`/`EINVAL`/`ENOSYS` on `RenameatxNp` falls back to exclusive-create + copy + fsync + remove-source; every other errno propagates unchanged. Package-level `var renameNoReplace = unix.RenameatxNp` added for injection.
- [x] 5.6 GREEN: implement fallback in `publish_immutable_darwin.go`.
- [x] 5.7 RED→GREEN E2E: `issue-1781`, `issue-1804` in `TestOrganicReviewPlatformFailSafes` — both `t.Skip` off-platform on Linux with a recorded reason; unit tests carry the real proof.
- [x] 5.8 Documented explicitly (test/skip comments) that the community must verify macOS managed-profile `/` restriction and ExFAT-hosted Git common dir; CI cannot.

## Phase 6 — Group C: Recovery graph (1744, 1816, 1782)

- [x] 6.1 Baseline: run `go test ./internal/reviewtransaction/...` and record the passing set before any change (gate for this commit).
- [x] 6.2/6.3 **SUPERSEDED by maintainer decision** (not implemented as originally worded). A first attempt added `compactRecoveryTargetKindAdmissible`/`errCompactRecoveryTargetKindIncompatible` exactly as designed and confirmed via the full sweep that it broke 7 pre-existing tests across 3 disposition families (RecoveryInvalidated, RecoveryEscalated/historicalFailedValidator, and the untargeted ordinary RecoveryScopeChanged/StateApproved path), all sharing root cause: `TargetExactRevision` and `TargetFixDiff` are load-bearing, currently-unconstrained kind values the design's admissible-shape list never accounted for. Reported per the honesty contract instead of guessing a wider list; the maintainer reverted this WIP and ruled kind freedom in recovery is deliberate, not a gap — the real boundary is identity-bound authorization, substantive scope change, and the base-diff base-tree pin, none of which is kind matching. `specs/review-findings-ledger/spec.md` was amended accordingly (kind-constraint clause replaced with the identity-bound-authority requirement + 4 negative scenarios).
- [x] 6.4 RED: 5 tests in new `internal/cli/review_recovery_target_kind_test.go` (not `TestReviewFacadeRecoverArgvPredicate` — named for the actual scenarios per the amended spec): `TestReviewRecoverEscalatedBaseDiffSuccessorOverCurrentChangesPredecessor` (primary 1744 RED, confirmed failing with "base-diff recovery requires matching --base-ref and --committed-only"), plus 4 negative regression proofs matching the amended spec's negative scenarios (`TestReviewRecoverEscalatedRequiresExactSuccessorBoundAuthorization`, `TestReviewRecoverInvalidatedRequiresInvalidatedPredecessor`, `TestReviewRecoverBaseDiffPredecessorStillBindsFrozenBaseTree`, `TestReviewRecoverArgvCoherenceStillRejectsMismatchedBaseRefFlags` — these 4 already passed pre-fix, confirming they exercise protections unaffected by the CLI kind pre-gate).
- [x] 6.5 GREEN: `review_facade.go` (current line ~689-691, shifted from the brief's :669-671 by earlier phases): removed only `baseDiff != *committedOnly`, kept `*committedOnly != (base != "")` verbatim. `compact_store.go` untouched (6.2/6.3 superseded).
- [x] 6.6 RED→GREEN E2E: `issue-1744` in new `TestOrganicReviewRecoveryGraph` (`e2e/organicruntime/organic_lifecycle_hardening_test.go`) — an escalated recovery over a current-changes predecessor requests a base-diff successor via `--base-ref`/`--committed-only` and reaches semantic admission; a mismatched argv shape in the same journey is still rejected by the surviving argv-coherence clause. Confirmed genuine RED by reverting only `review_facade.go` (`git stash` + rerun with `-count=1`).
- [x] 6.7 RED: `TestCompactReleaseScopeRecoveryParentCount` in new `internal/reviewtransaction/compact_release_scope_parent_test.go` — one-parent delivery commit accepted as HEAD; root commit (zero parents) refused with a typed message.
- [x] 6.8 GREEN: `compact_store.go:169-176` (`BuildReleaseScopeSnapshot`) relaxed from requiring `commit^2^{commit}` (a second parent) to requiring `commit^1^{commit}` (at least one parent); `compactReleaseScopeRecovery` (`:393-401`) untouched, remains the real authority gate. Existing merge-commit CLI test (`TestReviewRecoverReleaseScopeExpandsMergedSliceToFirstParentDiff`) still passes unmodified.
- [x] 6.9 RED→GREEN E2E: `issue-1816` — a genuine one-parent squash-merge delivery commit (built via `git merge --squash` + commit onto main, not a `--no-ff` merge) is accepted as release-scope HEAD. Confirmed genuine RED by reverting only `compact_store.go`.
- [x] 6.10 Confirmed anchor: `addCompactChainConvergence` (`compact_chain_test.go:919`) injects its extra receipt at `fixture.commits[0]` — the tree AFTER the FIRST segment, i.e. **mid-chain**, never at `fixture.base` (the true chain root). It therefore already correctly exercises the negative regression (a genuine convergence must stay rejected) and needed no re-pinning; `TestCompactPrePRChainRejectsForkConvergenceAndCycle/convergence` was left completely unmodified and still passes. The real 1782 repro required a separate, new fixture: three genuinely linear receipts X→A→B→C with the publication tracker moved to land exactly on A.
- [x] 6.11 RED: `TestSelectCompactPrePRChainRootIncomingExemption` — a linear 3-receipt chain (X→A→B→C, no fork, no real convergence) with the publication tracker (`origin/<branch>`) force-moved to commit A: confirmed genuinely failing with "compact receipt chain contains a convergence" before the fix.
- [x] 6.12 GREEN: `compact_chain.go:452-458` (`selectCompactPrePRChain`) — the selected path's own root (`path[0].receipt.BaseTree`) is now exempted entirely from the incoming-degree check (`continue` instead of `wantIncoming = 0`); every other path node still requires exactly one incoming edge. Fork guard (`:444-451`) and single-candidate guard (`:430-432`) untouched.
- [x] 6.13 RED→GREEN E2E: `issue-1782` in `TestOrganicReviewRecoveryGraph` — three real, separately-pushed deliveries (segments a/b/c) with the bare remote's `main` ref force-moved back to segment a's commit; `pre-pr --base-ref origin/main` now allows. Confirmed genuine RED by reverting only `compact_chain.go`. (The mid-chain-convergence control case is `TestCompactPrePRChainRejectsForkConvergenceAndCycle/convergence`, already covered at the unit level per 6.10 — not duplicated at E2E since it requires no new production-code path.)
- [x] 6.14 Regression gate: full `go test ./internal/reviewtransaction/... -count=1` green (117s) with all three Group C fixes together, zero test edits; `go test ./internal/cli -count=1` also green (95s); `go run ./internal/gofmtcheck` clean.

## Phase 7 — Group D: Store robustness (1813)

- [x] 7.1 RED: `TestCompactSemanticStateErrorTyped` in new `internal/reviewtransaction/compact_semantic_quarantine_test.go` — `Load()` wraps the anonymous `errors.New` into typed `*CompactSemanticStateError{LineageID,State,Problem}`. Confirmed genuinely RED via compile failure (`undefined: CompactSemanticStateError`) before the type existed.
- [x] 7.2 GREEN: new `CompactSemanticStateError` type in `compact.go` (near `Validate()`); the ONE construction site is `compact_store.go:1636-1638` (`parseCompactRecord`, called by `Load()`), wrapping `record.State.Validate()`'s error — not each individual branch inside `Validate()` itself, since the type must mark exactly "this came from Validate(), never checksum/IO/parse" for task 7.3's predicate, and `Validate()`'s many sub-checks (snapshot, findings, the `:399-412` switch, etc.) all funnel through this one call site.
- [x] 7.3 RED: `TestCompactLineageQuarantinable` — predicate true only when `errors.As` matches `*CompactSemanticStateError` AND state ∈ {Approved, Escalated, Invalidated} AND failure is semantic-validation class (never checksum/IO/parse); table-tested against nil, generic, and wrapped-non-semantic errors too.
- [x] 7.4 GREEN: implemented `compactLineageQuarantinable(err error) (*CompactSemanticStateError, bool)` in `compact.go`.
- [x] 7.5 RED: `TestCompactAuthorityLeavesQuarantine` — `CompactAuthorityLeaves` (`compact_store.go:558-560` area) and `target_status_projection.go`'s selector-free branch (`:54-65`) exclude one invalid TERMINAL lineage without affecting others; explicit-selector path (`:66-96`) still fails closed; the operated-on lineage's own `Load()` still fails closed. `DiscoverCompactStores` itself was left untouched (confirmed, per design, it never loads records — the poisoning lives entirely in the consumers).
- [x] 7.6 GREEN: wired the quarantine predicate at `compact_store.go`'s `CompactAuthorityLeaves` and `target_status_projection.go`'s selector-free branch only. **Non-obvious fix needed inside the selector-free branch**: `compactAuthorityLeaves`'s leaf selection iterates the STORE map it is given, not the records map — so the shared `storeByLineage` (needed unfiltered by the explicit-selector branch below it) could not be reused for the selector-free leaf computation; a separate, locally scoped `healthyStoreByLineage` map was required, or the quarantined lineage kept reappearing as a spurious "leaf" via the unfiltered store map even after being excluded from records.
- [x] 7.7 RED: `TestAuthorityInventoryDiagnosticQuarantine` — structured `AuthorityInventoryDiagnostic{Path, Problem: "quarantined-terminal-lineage: ..."}` surfaced on every status query, never a repeated stderr warning (verified two consecutive `InventoryAuthority` calls surface exactly one diagnostic, not an accumulating one); `report.Complete`/`report.Authoritative` NOT flipped; healthy lineages still enumerated normally; the quarantined lineage absent from `report.Entries`.
- [x] 7.8 GREEN: `inventoryLineage` (`status.go`) signature extended to return `(entry, locks, *AuthorityInventoryDiagnostic)`; its one call site in `inventoryVersion` diverts a non-nil diagnostic into `result.diagnostics` and skips appending that entry, instead of the prior unconditional `AuthorityStatusInvalid`.
- [x] 7.9 RED regression: **already covered by the existing, unmodified** `TestInventoryAuthorityReportsActiveMalformedAndMixedCollisionWithoutMutation` (malformed/unparseable `review-state.json`, a structural JSON-decode failure, never reaches `CompactSemanticStateError`) and `TestInventoryAuthorityKeepsMalformedInvalidatedAuthorityFailClosed` (a receipt-mismatch corruption, a different failure class entirely) — both verified still passing unmodified after the fix. Also added `TestCompactQuarantineNeverAppliesToNonTerminalOrStructuralCorruption`, an explicit end-to-end proof that a corrupt NON-terminal (Reviewing) lineage still fails closed through `Load()`, `CompactAuthorityLeaves`, the selector-free status path, and `InventoryAuthority` (flips Complete/Authoritative as before).
- [x] 7.10 RED→GREEN E2E: `issue-1813` in new `TestOrganicReviewStoreRobustness` — one invalid TERMINAL lineage among two healthy ones; `review status` (no `--contract`) diagnostic surfaced, `complete`/`authoritative` unflipped, healthy entries intact; a **third, freshly-started** healthy lineage proves `review start` stays fully operable with the quarantined lineage present in the same store; explicit `--lineage <quarantined>` status selector still fails closed (`applicability: corrupted`). **Deviation found via this E2E test, not anticipated by the design's 3-site citation**: `review start`'s bulk discovery scan (`StartCompactAuthority`, `compact_store.go:758-770`) is a THIRD "enumerate every store, treat one failure as fatal" consumer the design didn't cite by exact line — confirmed via a real RED failure ("load compact start authority: ...") when only the two originally-cited sites were fixed. Fixed with the identical quarantine-`continue` pattern. Separately verified `AbandonPristineCompactStore` and `ReclaimIncompleteCompactStore` (compact_abandon.go, compact_reclaim.go) never enumerate the full store list at all — both operate solely on their own explicit `request.LineageID` target directory — so neither was ever affected by 1813 and neither needed a change.
- [x] 7.11 Regression: covered by `TestCompactQuarantineNeverAppliesToNonTerminalOrStructuralCorruption` (7.9) plus the two pre-existing, unmodified `TestInventoryAuthority*` structural-corruption tests.

**Verification**: full `go test ./internal/reviewtransaction/... -count=1` green (~122s) with ALL Phase 7 changes together, zero test edits to any pre-existing test; `go test ./internal/cli -count=1` green (~95s); `go run ./internal/gofmtcheck` clean. Every RED for every new test confirmed genuine via `git stash push -- <files>` + `go test -count=1` + `git stash pop` before implementing GREEN.

## Regression Proof Tasks (explicit, cross-group)

- [x] R.1 Pre-push before push still `allow`: `TestReviewValidateReportsDisabledUnmanagedDeliveryWithoutReceipt`, `TestReviewValidateWithoutReceiptStillDeniesWhileReviewIsEnabled` — PASS.
- [x] R.2 `--committed-only` review start/gate byte-identical: `TestReviewRecoverRetainsCommittedOnlyBaseDiffAndIgnoresWorkspace` (pre-existing, unmodified) and new `TestReviewRecoverArgvCoherenceStillRejectsMismatchedBaseRefFlags` (both subtests) — PASS.
- [x] R.3 Governing-receipt allow unchanged: `TestReviewValidateKeepsGoverningReceiptAuthoritativeWhileDisabled` — PASS.
- [x] R.4 Disabled/unmanaged discovery codes byte-identical: `TestReviewValidateReportsDisabledUnmanagedDeliveryWithPriorReceipt` (both subtests), `TestReviewValidateReportsDisabledUnmanagedDeliveryOverDeliveredWorkspaceReceiptAtPrePush`, `TestReviewValidateDeniesDeliveredWorkspaceReceiptPrePushAsScopeMismatchWhileEnabled` — PASS.
- [x] R.5 Genuinely corrupted/ambiguous authority still fails closed: `TestReviewValidateKeepsFailingClosedOnCorruptedAuthorityWhileDisabledAtPrePush`, `TestReviewValidateKeepsFailingClosedOnCorruptedAuthorityWhileDisabled` (pre-existing, unmodified) + new Group D structural-corruption cases `TestCompactQuarantineNeverAppliesToNonTerminalOrStructuralCorruption` and `TestInventoryAuthorityReportsActiveMalformedAndMixedCollisionWithoutMutation` (pre-existing, unmodified) — all PASS.
- [x] R.6 A real mid-chain convergence still rejected: `TestCompactPrePRChainRejectsForkConvergenceAndCycle/convergence` (`addCompactChainConvergence` fixture, pre-existing, byte-for-byte unmodified) — PASS, still correctly rejects (`attempted=true, Result != GateAllow`).

## Added after community report

- [x] C.15 issue #1832 (dnlrsls): disabled pre-push gate fails with a typed `target_resolution_failed` denial instead of reporting `disabled/unmanaged` when no remote/upstream is configured, because `runReviewFacadeValidate` (`review_facade.go`) checked `*reviewtransaction.GateTargetResolutionError` before the disabled-disposition routing. RED: `TestReviewValidateReportsDisabledUnmanagedDeliveryWhenNoUpstreamConfigured` (`internal/cli/review_disabled_delivery_test.go`) and `TestOrganicReviewLifecycleErrorTyping/issue-1832` (`e2e/organicruntime/organic_lifecycle_hardening_test.go`) confirmed genuinely failing (exit 1, `target_resolution_failed`) by temporarily reverting only the new routing block in `review_facade.go` and rerunning. GREEN: added `ReviewReceiptTargetUnresolvable` discovery kind, extended `reviewReceiptDiscoveryIsUnmanagedWhileDisabled` to admit it, and checked the disabled/unmanaged disposition inside the existing `errors.As(compactErr, &targetResolution)` branch before falling through to the unchanged typed denial — chosen over deriving the target earlier because the disabled-vs-enabled reclassification already lives entirely at this one facade call site for every other discovery kind (missing/unrelated/scope-changed); duplicating that pattern keeps `reviewtransaction` gate/target derivation free of RDD-mode awareness. Pinning: `TestReviewValidateDeniesNoUpstreamTargetResolutionWhileEnabled` proves the enabled path stays byte-identical (exit 1, same fields, same `--base-ref <remote>/<branch>` message). Negative: `TestReviewValidateKeepsFailingClosedOnCorruptedAuthorityWhileDisabledNoUpstream` proves corrupted authority with no upstream and the switch off still fails closed as `authority_corrupted`, never `disabled/unmanaged`. Other gates: pre-commit/post-apply never derive a publication target (unaffected); pre-PR/release derive their target via `selectPrePRBoundary`, which returns untyped errors (never `*GateTargetResolutionError`) that already fall into the `authority_corrupted` bucket regardless of RDD mode — a separate, pre-existing classification gap out of scope for this fix and left unchanged. Verification: `go run ./internal/gofmtcheck` clean; `go test ./internal/cli ./internal/reviewtransaction/... -count=1` green; `go test ./e2e/organicruntime -count=1 -timeout=15m` green.

All six run and passed as real `go test -v` invocations in this apply session, not claimed.
